# Corrections Log

Append-only record of SQL validation failures from `sql_query_agent.py`, for `wiki_agent.py` to review on its next regeneration pass and address the documentation gap that caused each one.

## [2026-08-01] vendor_pricing — validation failed on first attempt

- Question: "what is the vendor price for vendor id = V00999"
- Wiki pages used: entities/vendors, relationships/vendor_pricing-vendors
- Rejected SQL: `SELECT vp.price FROM vendor_pricing vp JOIN vendors v ON vp.vendor_id = v.vendor_id WHERE v.vendor_id = 'V00999';`
- Validation error: vp.price does not exist on table `vendor_pricing`
- Outcome after one correction attempt: fixed — `SELECT vp.vendor_price FROM vendor_pricing vp JOIN vendors v ON vp.vendor_id = v.vendor_id WHERE v.vendor_id = 'V00999';`
- Suggested wiki fix: `entities/vendor_pricing` was NOT among the pages used, so its real column list wasn't in context when the query was written — check whether pages linking to `vendor_pricing` surface its key columns directly.
# Corrections Log

Append-only record of SQL validation failures from `sql_query_agent.py`, for `wiki_agent.py` to review on its next regeneration pass and address the documentation gap that caused each one.

## [2026-08-01] source_vendors — validation failed on first attempt

- Question: "what is the shippoint address for V00999"
- Wiki pages used: relationships/overview, entities/vendors, entities/vendor_shipping_points, relationships/vendor_shipping_points-vendors, entities/growth_opportunity_event, entities/items, entities/sites, entities/source_vendors
- Rejected SQL: `SELECT source_vendor_name, region, -- add any other relevant columns like address here FROM source_vendors WHERE source_vendor_id = 'V00999';`
- Validation error: relevant does not exist on table `source_vendors`
- Outcome after one correction attempt: STILL BROKEN — `SELECT source_vendor_name, ship_address FROM source_vendors JOIN vendor_addresses ON source_vendors.source_vendor_id = vendor_addresses.vendor_id WHERE source_vendor_name = 'V00999';`
- Suggested wiki fix: `entities/source_vendors` WAS in context but the model still invented a column — check whether its Column Reference Table clearly distinguishes similarly-named columns.

## [2026-08-01] vendors — validation failed on first attempt

- Question: "what is the shippoint address for V00999"
- Wiki pages used: relationships/overview, entities/vendors, relationships/vendor_shipping_points-vendors, entities/vendor_shipping_points, queries/contracts-by-shipping-point-per-site, entities/growth_opportunity_event, entities/items, entities/sites
- Rejected SQL: `-- Retrieve the shipping point address for vendor V00999. SELECT shippoint_address FROM vendors WHERE vendor_id = 'V00999';`
- Validation error: shippoint_address does not exist on table `vendors`
- Outcome after one correction attempt: STILL BROKEN — `SELECT shipping_point_address FROM vendor_shipping_points WHERE supplier_id = 'V00999';`
- Suggested wiki fix: `entities/vendors` WAS in context but the model still invented a column — check whether its Column Reference Table clearly distinguishes similarly-named columns.
# Corrections Log

Append-only record of SQL validation failures from `sql_query_agent.py`, for `wiki_agent.py` to review on its next regeneration pass and address the documentation gap that caused each one.

## [2026-08-01] unknown-table — validation failed on first attempt

- Question: "what is the shippoint address for vendor_id=V00999"
- Wiki pages used: relationships/overview, entities/vendors, entities/vendor_shipping_points, relationships/vendor_shipping_points-vendors, entities/growth_opportunity_event, entities/items, entities/sites, entities/source_vendors
- Rejected SQL: `SELECT vendor_address, vendor_city, vendor_state, vendor_zip FROM vendor_details WHERE vendor_id = 'V00999';`
- Validation error: table `vendor_details` referenced in FROM/JOIN does not exist in the schema
- Outcome after one correction attempt: STILL BROKEN — `SELECT source_vendor_id AS vendor_id, source_vendor_name AS vendor_name FROM source_vendors WHERE source_vendor_id = 'V00999';`
- Suggested wiki fix: `entities/unknown-table` was NOT among the pages used, so its real column list wasn't in context when the query was written — check whether pages linking to `unknown-table` surface its key columns directly.

## [2026-08-03] unknown-table — validation failed on first attempt

- Question: "what is the shippoint address for vendor_id=V00999"
- Wiki pages used: entities/vendor_shipping_points, entities/vendors, relationships/overview, entities/growth_opportunity_event, entities/items, entities/sites, entities/source_vendors, entities/site_vendor_assignments
- Rejected SQL: `SELECT sp.shipping_point_addr FROM ship_points AS sp JOIN vendors AS v ON sp.shipping_point_vendor_id = v.vendor_id WHERE v.vendor_id = 'V00999';`
- Validation error: table `ship_points` referenced in FROM/JOIN does not exist in the schema
- Outcome after one correction attempt: fixed — `-- There is no shippoint address available in the given tables. SELECT NULL AS shippoint_address WHERE 'V00999' IS NOT NULL;`
- Suggested wiki fix: `entities/unknown-table` was NOT among the pages used, so its real column list wasn't in context when the query was written — check whether pages linking to `unknown-table` surface its key columns directly.

## [2026-08-03] unknown-table — validation failed on first attempt

- Question: "what is the ship_point_address for vendor_id=V00999"
- Wiki pages used: entities/vendor_shipping_points, entities/vendors, relationships/overview, entities/growth_opportunity_event, entities/items, entities/sites, entities/source_vendors, entities/site_vendor_assignments
- Rejected SQL: `SELECT ship_point_address FROM vendor_locations WHERE vendor_id = 'V00999';`
- Validation error: table `vendor_locations` referenced in FROM/JOIN does not exist in the schema
- Outcome after one correction attempt: fixed — `SELECT sv.source_vendor_id, sv.source_vendor_name, sv.source_vendor_group, sv.region AS vendor_region, sv.created_timestamp, ss.site AS assigned_site, ss.is_primary AS primary_flag, ss.assigned_date FROM source_vendors AS sv LEFT JOIN site_vendor_assignments AS ss ON sv.source_vendor_id = ss.vendor_id WHERE sv.source_vendor_id = 'V00999';`
- Suggested wiki fix: `entities/unknown-table` was NOT among the pages used, so its real column list wasn't in context when the query was written — check whether pages linking to `unknown-table` surface its key columns directly.

## [2026-08-03] unknown-table — validation failed on first attempt

- Question: "what is the ship_point_address for vendor_id=V00999"
- Wiki pages used: entities/vendor_shipping_points, relationships/vendor_shipping_points-vendors, entities/vendors, relationships/overview, entities/growth_opportunity_event, entities/items, entities/sites, entities/source_vendors
- Rejected SQL: `SELECT ship_point_address FROM vendor_info WHERE vendor_id = 'V00999';`
- Validation error: table `vendor_info` referenced in FROM/JOIN does not exist in the schema
- Outcome after one correction attempt: STILL BROKEN — `SELECT s.source_vendor_id, s.source_vendor_name, s.source_vendor_group FROM source_vendors s WHERE s.source_vendor_id = 'V00999';`
- Suggested wiki fix: `entities/unknown-table` was NOT among the pages used, so its real column list wasn't in context when the query was written — check whether pages linking to `unknown-table` surface its key columns directly.

## [2026-08-03] ship_points — validation failed on first attempt

- Question: "what is the shippoint address for vendor_id=V00999"
- Wiki pages used: entities/vendor_shipping_points, relationships/overview, entities/vendor_contracts, entities/source_vendors, entities/vendors, entities/growth_opportunity_event, entities/items, entities/sites
- Rejected SQL: `SELECT sp.ship_point_name, va.vendor_address_city AS city, va.vendor_address_state AS state, va.vendor_address_zip AS zip FROM ship_points AS sp JOIN vendors AS v ON sp.ship_point_vendor_id = v.vendor_id LEFT JOIN vendor_addresses AS va ON va.vendor_id = v.vendor_id WHERE v.vendor_id = 'V00999';`
- Validation error: table `ship_points` referenced in FROM/JOIN does not exist in the schema
- All rejected attempts:
  1. `SELECT sp.ship_point_name, va.vendor_address_city AS city, va.vendor_address_state AS state, va.vendor_address_zip AS zip FROM ship_points AS sp JOIN vendors AS v ON sp.ship_point_vendor_id = v.vendor_id LEFT JOIN vendor_addresses AS va ON va.vendor_id = v.vendor_id WHERE v.vendor_id = 'V00999';` — table `ship_points` referenced in FROM/JOIN does not exist in the schema
  2. `-- No data can be returned because vendor_id / ship_point_address columns are missing from the supplied tables. -- Hypothetically, if a vendors table existed with those fields, the query would look like this: SELECT vendor_id, ship_point_address FROM vendors WHERE vendor_id = 'V00999';` — table `the` referenced in FROM/JOIN does not exist in the schema
  3. `-- Hypothetical example (requires a vendors table) SELECT vendor_id, shippoint_address FROM vendors WHERE vendor_id = 'V00999';` — shippoint_address does not exist on table `vendors`
- Outcome after 2 correction attempt(s): STILL BROKEN — `-- Hypothetical example (requires a vendors table) SELECT vendor_id, shippoint_address FROM vendors WHERE vendor_id = 'V00999';`
- Suggested wiki fix: `entities/ship_points` was NOT among the pages used, so its real column list wasn't in context when the query was written — check whether pages linking to `ship_points` surface its key columns directly.

## [2026-08-03] events_5 — validation failed on first attempt

- Question: "What is the average price of items sold by vendor Coastal Seafood Traders?"
- Wiki pages used: entities/vendors, entities/vendor_pricing, relationships/overview, entities/growth_opportunity_event, entities/items, entities/sites, entities/source_vendors, entities/site_vendor_assignments
- Rejected SQL: `SELECT AVG(CAST(vendor_price AS NUMERIC)) AS avg_vendor_price FROM events_5 WHERE vendor_name = 'Coastal Seafood Traders';`
- Validation error: table `events_5` referenced in FROM/JOIN does not exist in the schema
- All rejected attempts:
  1. `SELECT AVG(CAST(vendor_price AS NUMERIC)) AS avg_vendor_price FROM events_5 WHERE vendor_name = 'Coastal Seafood Traders';` — table `events_5` referenced in FROM/JOIN does not exist in the schema
  2. `SELECT AVG(CAST(vendor_price AS NUMERIC)) AS avg_vendor_price FROM events_5 WHERE vendor_name = 'Coastal Seafood Traders';` — table `events_5` referenced in FROM/JOIN does not exist in the schema
  3. `SELECT AVG(e.vendor_price::numeric) AS average_item_price FROM events_5 e JOIN source_vendors s ON e.vendor_name = s.source_vendor_name WHERE s.source_vendor_name = 'Coastal Seafood Traders';` — table `events_5` referenced in FROM/JOIN does not exist in the schema
- Outcome after 2 correction attempt(s): STILL BROKEN — `SELECT AVG(e.vendor_price::numeric) AS average_item_price FROM events_5 e JOIN source_vendors s ON e.vendor_name = s.source_vendor_name WHERE s.source_vendor_name = 'Coastal Seafood Traders';`
- Suggested wiki fix: `entities/events_5` was NOT among the pages used, so its real column list wasn't in context when the query was written — check whether pages linking to `events_5` surface its key columns directly.
# Corrections Log

Append-only record of SQL validation failures from `sql_query_agent.py`, for `wiki_agent.py` to review on its next regeneration pass and address the documentation gap that caused each one.

## [2026-08-03] po — validation failed on first attempt

- Question: "Which sites have both a site_vendor_assignments primary vendor AND active sales_orders, and what's the gap between what they buy (PO $) and what they sell (invoice $) in the same month?"
- Wiki pages used: entities/site_vendor_assignments, entities/sales_orders, entities/purchase_orders, entities/sales_invoices, relationships/site_vendor_assignments-sites, relationships/sales_orders-sites, relationships/sales_invoices-sales_orders, relationships/overview
- Rejected SQL: `SELECT sva.site, COALESCE(SUM(po.total_amount), 0) AS total_po_amount, COALESCE(SUM(si.total_amount), 0) AS total_invoice_amount, (COALESCE(SUM(si.total_amount), 0) - COALESCE(SUM(po.total_amount), 0)) AS gap FROM site_vendor_assignments sva LEFT JOIN sales_orders so ON sva.site = so.site LEFT JOIN sales_invoices si ON so.order_id = si.order_id LEFT JOIN purchase_orders po ON sva.site = po.site AND EXTRACT(YEAR FROM po.po_date) = EXTRACT(YEAR FROM si.invoice_date) AND EXTRACT(MONTH FROM po.po_date) = EXTRACT(MONTH FROM si.invoice_date) WHERE sva.is_primary = 'Y' GROUP BY sva.site HAVING SUM(CASE WHEN so.status = 'Open' THEN 1 ELSE 0 END) > 0;`
- Validation error: table `po` referenced in FROM/JOIN does not exist in the schema
- All rejected attempts:
  1. `SELECT sva.site, COALESCE(SUM(po.total_amount), 0) AS total_po_amount, COALESCE(SUM(si.total_amount), 0) AS total_invoice_amount, (COALESCE(SUM(si.total_amount), 0) - COALESCE(SUM(po.total_amount), 0)) AS gap FROM site_vendor_assignments sva LEFT JOIN sales_orders so ON sva.site = so.site LEFT JOIN sales_invoices si ON so.order_id = si.order_id LEFT JOIN purchase_orders po ON sva.site = po.site AND EXTRACT(YEAR FROM po.po_date) = EXTRACT(YEAR FROM si.invoice_date) AND EXTRACT(MONTH FROM po.po_date) = EXTRACT(MONTH FROM si.invoice_date) WHERE sva.is_primary = 'Y' GROUP BY sva.site HAVING SUM(CASE WHEN so.status = 'Open' THEN 1 ELSE 0 END) > 0;` — table `po` referenced in FROM/JOIN does not exist in the schema
  2. `SELECT sva.site, COALESCE(SUM(po.total_amount), 0) AS total_po_amount, COALESCE(SUM(si.total_amount), 0) AS total_invoice_amount, (COALESCE(SUM(si.total_amount), 0) - COALESCE(SUM(po.total_amount), 0)) AS gap FROM site_vendor_assignments sva JOIN sales_orders so ON sva.site = so.site JOIN sales_invoices si ON so.order_id = si.order_id LEFT JOIN ( SELECT so.site, SUM(pi.total_amount) AS total_amount FROM sales_orders so JOIN sales_invoices pi ON so.order_id = pi.order_id WHERE so.status = 'Open' GROUP BY so.site, EXTRACT(YEAR FROM pi.invoice_date), EXTRACT(MONTH FROM pi.invoice_date) ) po ON po.site = sva.site AND EXTRACT(YEAR FROM si.invoice_date) = EXTRACT(YEAR FROM po.po_date) AND EXTRACT(MONTH FROM si.invoice_date) = EXTRACT(MONTH FROM po.po_date) WHERE sva.is_primary = 'Y' GROUP BY sva.site HAVING SUM(CASE WHEN so.status = 'Open' THEN 1 ELSE 0 END) > 0;` — table `pi` referenced in FROM/JOIN does not exist in the schema
  3. `SELECT sva.site, COALESCE(SUM(po.total_amount), 0) AS total_po_amount, COALESCE(SUM(si.total_amount), 0) AS total_invoice_amount, (COALESCE(SUM(si.total_amount), 0) - COALESCE(SUM(po.total_amount), 0)) AS gap FROM site_vendor_assignments sva LEFT JOIN sales_orders so ON sva.site = so.site LEFT JOIN sales_invoices si ON so.order_id = si.order_id LEFT JOIN purchase_orders po ON sva.site = po.site WHERE sva.is_primary = 'Y' AND so.status = 'Open' GROUP BY sva.site HAVING COUNT(so.order_id) > 0;` — po.total_amount does not exist on table `purchase_orders`
- Outcome after 2 correction attempt(s): STILL BROKEN — `SELECT sva.site, COALESCE(SUM(po.total_amount), 0) AS total_po_amount, COALESCE(SUM(si.total_amount), 0) AS total_invoice_amount, (COALESCE(SUM(si.total_amount), 0) - COALESCE(SUM(po.total_amount), 0)) AS gap FROM site_vendor_assignments sva LEFT JOIN sales_orders so ON sva.site = so.site LEFT JOIN sales_invoices si ON so.order_id = si.order_id LEFT JOIN purchase_orders po ON sva.site = po.site WHERE sva.is_primary = 'Y' AND so.status = 'Open' GROUP BY sva.site HAVING COUNT(so.order_id) > 0;`
- Suggested wiki fix: `entities/po` was NOT among the pages used, so its real column list wasn't in context when the query was written — check whether pages linking to `po` surface its key columns directly.

## [2026-08-03] po_receipt_lines — validation failed on first attempt

- Question: "Which sites have both a site_vendor_assignments primary vendor AND active sales_orders, and what's the gap between what they buy (PO $) and what they sell (invoice $) in the same month?"
- Wiki pages used: entities/site_vendor_assignments, entities/sales_orders, entities/purchase_order_lines, entities/sales_invoice_lines, queries/site-vendor-assignments-primary-status, queries/sales-orders-shipments, queries/customer-sales-orders, queries/unshipped-items-purchase-orders
- Rejected SQL: `WITH po_totals AS ( SELECT site, EXTRACT(YEAR FROM po_date) AS year, EXTRACT(MONTH FROM po_date) AS month, SUM(po_receipt_lines.qty_received * purchase_order_lines.unit_cost) AS total_po_amount FROM purchase_orders JOIN purchase_order_lines ON purchase_orders.po_id = purchase_order_lines.po_id JOIN po_receipt_lines ON purchase_orders.po_id = po_receipt_lines.po_id GROUP BY site, year, month ), invoice_totals AS ( SELECT customers.site, EXTRACT(YEAR FROM sales_invoices.invoice_date) AS year, EXTRACT(MONTH FROM sales_invoices.invoice_date) AS month, SUM(sales_invoice_lines.qty_invoiced * sales_invoice_lines.unit_price) AS total_invoice_amount FROM sales_invoices JOIN sales_invoice_lines ON sales_invoices.invoice_id = sales_invoice_lines.invoice_id JOIN sales_orders ON sales_invoices.order_id = sales_orders.order_id JOIN customers ON sales_orders.customer_id = customers.customer_id GROUP BY customers.site, year, month ) SELECT sva.site, COALESCE(SUM(po_totals.total_po_amount), 0) AS total_po_amount, COALESCE(SUM(invoice_totals.total_invoice_amount), 0) AS total_invoice_amount, (COALESCE(SUM(invoice_totals.total_invoice_amount), 0) - COALESCE(SUM(po_totals.total_po_amount), 0)) AS gap FROM site_vendor_assignments sva JOIN sales_orders so ON sva.site = so.site LEFT JOIN po_totals ON sva.site = po_totals.site LEFT JOIN invoice_totals ON sva.site = invoice_totals.site AND po_totals.year = invoice_totals.year AND po_totals.month = invoice_totals.month WHERE sva.is_primary = 'Y' AND so.status = 'Open' GROUP BY sva.site HAVING COUNT(so.order_id) > 0;`
- Validation error: po_receipt_lines.po_id does not exist on table `po_receipt_lines`
- All rejected attempts:
  1. `WITH po_totals AS ( SELECT site, EXTRACT(YEAR FROM po_date) AS year, EXTRACT(MONTH FROM po_date) AS month, SUM(po_receipt_lines.qty_received * purchase_order_lines.unit_cost) AS total_po_amount FROM purchase_orders JOIN purchase_order_lines ON purchase_orders.po_id = purchase_order_lines.po_id JOIN po_receipt_lines ON purchase_orders.po_id = po_receipt_lines.po_id GROUP BY site, year, month ), invoice_totals AS ( SELECT customers.site, EXTRACT(YEAR FROM sales_invoices.invoice_date) AS year, EXTRACT(MONTH FROM sales_invoices.invoice_date) AS month, SUM(sales_invoice_lines.qty_invoiced * sales_invoice_lines.unit_price) AS total_invoice_amount FROM sales_invoices JOIN sales_invoice_lines ON sales_invoices.invoice_id = sales_invoice_lines.invoice_id JOIN sales_orders ON sales_invoices.order_id = sales_orders.order_id JOIN customers ON sales_orders.customer_id = customers.customer_id GROUP BY customers.site, year, month ) SELECT sva.site, COALESCE(SUM(po_totals.total_po_amount), 0) AS total_po_amount, COALESCE(SUM(invoice_totals.total_invoice_amount), 0) AS total_invoice_amount, (COALESCE(SUM(invoice_totals.total_invoice_amount), 0) - COALESCE(SUM(po_totals.total_po_amount), 0)) AS gap FROM site_vendor_assignments sva JOIN sales_orders so ON sva.site = so.site LEFT JOIN po_totals ON sva.site = po_totals.site LEFT JOIN invoice_totals ON sva.site = invoice_totals.site AND po_totals.year = invoice_totals.year AND po_totals.month = invoice_totals.month WHERE sva.is_primary = 'Y' AND so.status = 'Open' GROUP BY sva.site HAVING COUNT(so.order_id) > 0;` — po_receipt_lines.po_id does not exist on table `po_receipt_lines`
- Outcome after 0 correction attempt(s): fixed — `WITH po_totals AS ( SELECT site, EXTRACT(YEAR FROM po_date) AS year, EXTRACT(MONTH FROM po_date) AS month, SUM(po_receipt_lines.qty_received * purchase_order_lines.unit_cost) AS total_po_amount FROM purchase_orders JOIN purchase_order_lines ON purchase_orders.po_id = purchase_order_lines.po_id JOIN po_receipt_lines ON purchase_orders.po_id = po_receipt_lines.po_id GROUP BY site, year, month ), invoice_totals AS ( SELECT customers.site, EXTRACT(YEAR FROM sales_invoices.invoice_date) AS year, EXTRACT(MONTH FROM sales_invoices.invoice_date) AS month, SUM(sales_invoice_lines.qty_invoiced * sales_invoice_lines.unit_price) AS total_invoice_amount FROM sales_invoices JOIN sales_invoice_lines ON sales_invoices.invoice_id = sales_invoice_lines.invoice_id JOIN sales_orders ON sales_invoices.order_id = sales_orders.order_id JOIN customers ON sales_orders.customer_id = customers.customer_id GROUP BY customers.site, year, month ) SELECT sva.site, COALESCE(SUM(po_totals.total_po_amount), 0) AS total_po_amount, COALESCE(SUM(invoice_totals.total_invoice_amount), 0) AS total_invoice_amount, (COALESCE(SUM(invoice_totals.total_invoice_amount), 0) - COALESCE(SUM(po_totals.total_po_amount), 0)) AS gap FROM site_vendor_assignments sva JOIN sales_orders so ON sva.site = so.site LEFT JOIN po_totals ON sva.site = po_totals.site LEFT JOIN invoice_totals ON sva.site = invoice_totals.site AND po_totals.year = invoice_totals.year AND po_totals.month = invoice_totals.month WHERE sva.is_primary = 'Y' AND so.status = 'Open' GROUP BY sva.site HAVING COUNT(so.order_id) > 0;`
- Suggested wiki fix: `entities/po_receipt_lines` was NOT among the pages used, so its real column list wasn't in context when the query was written — check whether pages linking to `po_receipt_lines` surface its key columns directly.

## [2026-08-03] vendor_invoice_lines — validation failed on first attempt

- Question: " "Which sites have both a site_vendor_assignments primary vendor AND active sales_orders, and what's the gap between what they buy (PO $) and what they sell (invoice $) in the same month?"
- Wiki pages used: entities/site_vendor_assignments, entities/sales_orders, entities/sales_invoices, entities/purchase_orders, queries/site-vendor-assignments-primary-status, relationships/overview, entities/po_receipts, entities/vendor_contracts
- Rejected SQL: `WITH site_primary_vendor AS ( SELECT DISTINCT site FROM site_vendor_assignments WHERE is_primary = 'Y' ), site_sales AS ( SELECT so.site, SUM(si.total_amount) AS total_sales, DATE_TRUNC('month', si.invoice_date) AS sales_month FROM sales_orders so JOIN sales_invoices si ON so.order_id = si.order_id WHERE so.status = 'Open' GROUP BY so.site, DATE_TRUNC('month', si.invoice_date) ), site_purchases AS ( SELECT po.site, SUM(po_amount) AS total_purchases, DATE_TRUNC('month', po.po_date) AS po_month FROM purchase_orders po JOIN ( SELECT po_id, SUM(vil.total_amount) AS po_amount FROM vendor_invoice_lines vil GROUP BY po_id ) vpo ON po.po_id = vpo.po_id GROUP BY po.site, DATE_TRUNC('month', po.po_date) ) SELECT spv.site, ss.sales_month, ss.total_sales, sp.total_purchases, COALESCE(sp.total_purchases, 0) - COALESCE(ss.total_sales, 0) AS gap FROM site_primary_vendor spv JOIN site_sales ss ON spv.site = ss.site LEFT JOIN site_purchases sp ON spv.site = sp.site AND ss.sales_month = sp.po_month;`
- Validation error: vil.total_amount does not exist on table `vendor_invoice_lines`
- All rejected attempts:
  1. `WITH site_primary_vendor AS ( SELECT DISTINCT site FROM site_vendor_assignments WHERE is_primary = 'Y' ), site_sales AS ( SELECT so.site, SUM(si.total_amount) AS total_sales, DATE_TRUNC('month', si.invoice_date) AS sales_month FROM sales_orders so JOIN sales_invoices si ON so.order_id = si.order_id WHERE so.status = 'Open' GROUP BY so.site, DATE_TRUNC('month', si.invoice_date) ), site_purchases AS ( SELECT po.site, SUM(po_amount) AS total_purchases, DATE_TRUNC('month', po.po_date) AS po_month FROM purchase_orders po JOIN ( SELECT po_id, SUM(vil.total_amount) AS po_amount FROM vendor_invoice_lines vil GROUP BY po_id ) vpo ON po.po_id = vpo.po_id GROUP BY po.site, DATE_TRUNC('month', po.po_date) ) SELECT spv.site, ss.sales_month, ss.total_sales, sp.total_purchases, COALESCE(sp.total_purchases, 0) - COALESCE(ss.total_sales, 0) AS gap FROM site_primary_vendor spv JOIN site_sales ss ON spv.site = ss.site LEFT JOIN site_purchases sp ON spv.site = sp.site AND ss.sales_month = sp.po_month;` — vil.total_amount does not exist on table `vendor_invoice_lines`
- Outcome after 0 correction attempt(s): fixed — `WITH site_primary_vendor AS ( SELECT DISTINCT site FROM site_vendor_assignments WHERE is_primary = 'Y' ), site_sales AS ( SELECT so.site, SUM(si.total_amount) AS total_sales, DATE_TRUNC('month', si.invoice_date) AS sales_month FROM sales_orders so JOIN sales_invoices si ON so.order_id = si.order_id WHERE so.status = 'Open' GROUP BY so.site, DATE_TRUNC('month', si.invoice_date) ), site_purchases AS ( SELECT po.site, SUM(po_amount) AS total_purchases, DATE_TRUNC('month', po.po_date) AS po_month FROM purchase_orders po JOIN ( SELECT po_id, SUM(vil.total_amount) AS po_amount FROM vendor_invoice_lines vil GROUP BY po_id ) vpo ON po.po_id = vpo.po_id GROUP BY po.site, DATE_TRUNC('month', po.po_date) ) SELECT spv.site, ss.sales_month, ss.total_sales, sp.total_purchases, COALESCE(sp.total_purchases, 0) - COALESCE(ss.total_sales, 0) AS gap FROM site_primary_vendor spv JOIN site_sales ss ON spv.site = ss.site LEFT JOIN site_purchases sp ON spv.site = sp.site AND ss.sales_month = sp.po_month;`
- Suggested wiki fix: `entities/vendor_invoice_lines` was NOT among the pages used, so its real column list wasn't in context when the query was written — check whether pages linking to `vendor_invoice_lines` surface its key columns directly.

## [2026-08-03] sales_returns — validation failed on first attempt

- Question: "Which customers have a return-to-invoice dollar ratio above the average for their region (via sites.region), AND are within 90% of their customer_credit_terms.credit_limit based on unpaid sales_invoices?"
- Wiki pages used: entities/customers, entities/sales_returns, entities/sales_invoices, entities/customer_credit_terms, entities/sites, relationships/sales_returns-sales_invoices, relationships/sales_orders-customers, relationships/overview
- Rejected SQL: `WITH regional_average_ratio AS ( SELECT c.customer_id, s.region, COALESCE(SUM(sr.total_return_amount) / NULLIF(SUM(si.total_amount), 0), 0) AS return_to_invoice_ratio FROM customers c JOIN sales_invoices si ON c.customer_id = si.customer_id LEFT JOIN sales_returns sr ON si.invoice_id = sr.invoice_id JOIN sites s ON c.site = s.site WHERE si.status = 'Unpaid' GROUP BY c.customer_id, s.region ), average_by_region AS ( SELECT region, AVG(return_to_invoice_ratio) AS avg_ratio FROM regional_average_ratio GROUP BY region ), total_unpaid_invoice AS ( SELECT customer_id, SUM(total_amount) AS total_unpaid FROM sales_invoices WHERE status = 'Unpaid' GROUP BY customer_id ) SELECT ra.customer_id FROM regional_average_ratio ra JOIN average_by_region ar ON ra.region = ar.region JOIN total_unpaid_invoice tui ON ra.customer_id = tui.customer_id JOIN customer_credit_terms cct ON ra.customer_id = cct.customer_id WHERE ra.return_to_invoice_ratio > ar.avg_ratio AND tui.total_unpaid >= 0.9 * cct.credit_limit;`
- Validation error: sr.total_return_amount does not exist on table `sales_returns`
- All rejected attempts:
  1. `WITH regional_average_ratio AS ( SELECT c.customer_id, s.region, COALESCE(SUM(sr.total_return_amount) / NULLIF(SUM(si.total_amount), 0), 0) AS return_to_invoice_ratio FROM customers c JOIN sales_invoices si ON c.customer_id = si.customer_id LEFT JOIN sales_returns sr ON si.invoice_id = sr.invoice_id JOIN sites s ON c.site = s.site WHERE si.status = 'Unpaid' GROUP BY c.customer_id, s.region ), average_by_region AS ( SELECT region, AVG(return_to_invoice_ratio) AS avg_ratio FROM regional_average_ratio GROUP BY region ), total_unpaid_invoice AS ( SELECT customer_id, SUM(total_amount) AS total_unpaid FROM sales_invoices WHERE status = 'Unpaid' GROUP BY customer_id ) SELECT ra.customer_id FROM regional_average_ratio ra JOIN average_by_region ar ON ra.region = ar.region JOIN total_unpaid_invoice tui ON ra.customer_id = tui.customer_id JOIN customer_credit_terms cct ON ra.customer_id = cct.customer_id WHERE ra.return_to_invoice_ratio > ar.avg_ratio AND tui.total_unpaid >= 0.9 * cct.credit_limit;` — sr.total_return_amount does not exist on table `sales_returns`
  2. `WITH item_return_values AS ( SELECT sr.return_id, sri.item_no, sri.qty_returned, sil.price AS item_price FROM sales_return_lines sri JOIN sales_returns sr ON sri.return_id = sr.return_id JOIN sales_invoice_lines sil ON sil.invoice_id = sr.invoice_id AND sil.item_no = sri.item_no ), return_value_per_invoice AS ( SELECT sr.invoice_id, SUM(sri.qty_returned * sil.price) AS total_return_value FROM sales_returns sr JOIN sales_return_lines sri ON sr.return_id = sri.return_id JOIN sales_invoice_lines sil ON sil.invoice_id = sr.invoice_id AND sil.item_no = sri.item_no GROUP BY sr.invoice_id ), regional_average_ratio AS ( SELECT c.customer_id, s.region, COALESCE(SUM(rvpi.total_return_value) / NULLIF(SUM(si.total_amount), 0), 0) AS return_to_invoice_ratio FROM customers c JOIN sales_invoices si ON c.customer_id = si.customer_id LEFT JOIN return_value_per_invoice rvpi ON si.invoice_id = rvpi.invoice_id JOIN sites s ON c.site = s.site WHERE si.status = 'Unpaid' GROUP BY c.customer_id, s.region ), average_by_region AS ( SELECT region, AVG(return_to_invoice_ratio) AS avg_ratio FROM regional_average_ratio GROUP BY region ), total_unpaid_invoice AS ( SELECT customer_id, SUM(total_amount) AS total_unpaid FROM sales_invoices WHERE status = 'Unpaid' GROUP BY customer_id ) SELECT ra.customer_id FROM regional_average_ratio ra JOIN average_by_region ar ON ra.region = ar.region JOIN total_unpaid_invoice tui ON ra.customer_id = tui.customer_id JOIN customer_credit_terms cct ON ra.customer_id = cct.customer_id WHERE ra.return_to_invoice_ratio > ar.avg_ratio AND tui.total_unpaid >= 0.9 * cct.credit_limit;` — sil.price does not exist on table `sales_invoice_lines`
  3. `WITH return_value_per_invoice AS ( SELECT sr.invoice_id, SUM(srl.qty_returned * sil.unit_price) AS total_return_value FROM sales_returns sr JOIN sales_return_lines srl ON sr.return_id = srl.return_id JOIN sales_invoice_lines sil ON sr.invoice_id = sil.invoice_id AND srl.item_no = sil.item_no GROUP BY sr.invoice_id ), regional_average_ratio AS ( SELECT c.customer_id, c.region, COALESCE(SUM(rvpi.total_return_value) / NULLIF(SUM(si.total_amount), 0), 0) AS return_to_invoice_ratio FROM customers c JOIN sales_invoices si ON c.customer_id = si.customer_id LEFT JOIN return_value_per_invoice rvpi ON si.invoice_id = rvpi.invoice_id WHERE si.status = 'Unpaid' GROUP BY c.customer_id, c.region ), average_by_region AS ( SELECT region, AVG(return_to_invoice_ratio) AS avg_ratio FROM regional_average_ratio GROUP BY region ), total_unpaid_invoice AS ( SELECT customer_id, SUM(total_amount) AS total_unpaid FROM sales_invoices WHERE status = 'Unpaid' GROUP BY customer_id ) SELECT ra.customer_id FROM regional_average_ratio ra JOIN average_by_region ar ON ra.region = ar.region JOIN total_unpaid_invoice tui ON ra.customer_id = tui.customer_id JOIN customer_credit_terms cct ON ra.customer_id = cct.customer_id WHERE ra.return_to_invoice_ratio > ar.avg_ratio AND tui.total_unpaid >= 0.9 * cct.credit_limit;` — si.customer_id does not exist on table `sales_invoices`
- Outcome after 2 correction attempt(s): STILL BROKEN — `WITH return_value_per_invoice AS ( SELECT sr.invoice_id, SUM(srl.qty_returned * sil.unit_price) AS total_return_value FROM sales_returns sr JOIN sales_return_lines srl ON sr.return_id = srl.return_id JOIN sales_invoice_lines sil ON sr.invoice_id = sil.invoice_id AND srl.item_no = sil.item_no GROUP BY sr.invoice_id ), regional_average_ratio AS ( SELECT c.customer_id, c.region, COALESCE(SUM(rvpi.total_return_value) / NULLIF(SUM(si.total_amount), 0), 0) AS return_to_invoice_ratio FROM customers c JOIN sales_invoices si ON c.customer_id = si.customer_id LEFT JOIN return_value_per_invoice rvpi ON si.invoice_id = rvpi.invoice_id WHERE si.status = 'Unpaid' GROUP BY c.customer_id, c.region ), average_by_region AS ( SELECT region, AVG(return_to_invoice_ratio) AS avg_ratio FROM regional_average_ratio GROUP BY region ), total_unpaid_invoice AS ( SELECT customer_id, SUM(total_amount) AS total_unpaid FROM sales_invoices WHERE status = 'Unpaid' GROUP BY customer_id ) SELECT ra.customer_id FROM regional_average_ratio ra JOIN average_by_region ar ON ra.region = ar.region JOIN total_unpaid_invoice tui ON ra.customer_id = tui.customer_id JOIN customer_credit_terms cct ON ra.customer_id = cct.customer_id WHERE ra.return_to_invoice_ratio > ar.avg_ratio AND tui.total_unpaid >= 0.9 * cct.credit_limit;`
- Suggested wiki fix: `entities/sales_returns` WAS in context but the model still invented a column/table — check whether its Column Reference Table, Common Query Examples, or Known Query Gotchas clearly distinguish similarly-named columns.

## [2026-08-03] sales_returns — validation failed on first attempt

- Question: "Which customers have a return-to-invoice dollar ratio above the average for their region (via sites.region), AND are within 90% of their customer_credit_terms.credit_limit based on unpaid sales_invoices?"
- Wiki pages used: entities/customers, entities/sales_returns, entities/sales_invoices, entities/customer_credit_terms, relationships/sales_returns-sales_invoices, relationships/sales_orders-customers, relationships/customer_credit_terms-customers, queries/customers-credit-holds
- Rejected SQL: `WITH total_sales_and_returns AS ( SELECT c.customer_id, c.region, COALESCE(SUM(sr.total_return_value) / NULLIF(SUM(si.total_amount), 0), 0) AS return_to_invoice_ratio FROM customers c JOIN sales_invoices si ON c.customer_id = si.customer_id LEFT JOIN ( SELECT sr.invoice_id, SUM(srl.qty_returned * sil.unit_price) AS total_return_value FROM sales_returns sr JOIN sales_return_lines srl ON sr.return_id = srl.return_id JOIN sales_invoice_lines sil ON sr.invoice_id = sil.invoice_id AND srl.item_no = sil.item_no GROUP BY sr.invoice_id ) sr ON si.invoice_id = sr.invoice_id WHERE si.status = 'Unpaid' GROUP BY c.customer_id, c.region ), average_ratios_by_region AS ( SELECT region, AVG(return_to_invoice_ratio) AS avg_ratio FROM total_sales_and_returns GROUP BY region ), unpaid_invoices AS ( SELECT customer_id, SUM(total_amount) AS total_unpaid FROM sales_invoices WHERE status = 'Unpaid' GROUP BY customer_id ) SELECT tsr.customer_id FROM total_sales_and_returns tsr JOIN average_ratios_by_region arbr ON tsr.region = arbr.region JOIN unpaid_invoices ui ON tsr.customer_id = ui.customer_id JOIN customer_credit_terms cct ON tsr.customer_id = cct.customer_id WHERE tsr.return_to_invoice_ratio > arbr.avg_ratio AND ui.total_unpaid >= 0.9 * cct.credit_limit;`
- Validation error: sr.total_return_value does not exist on table `sales_returns`
- All rejected attempts:
  1. `WITH total_sales_and_returns AS ( SELECT c.customer_id, c.region, COALESCE(SUM(sr.total_return_value) / NULLIF(SUM(si.total_amount), 0), 0) AS return_to_invoice_ratio FROM customers c JOIN sales_invoices si ON c.customer_id = si.customer_id LEFT JOIN ( SELECT sr.invoice_id, SUM(srl.qty_returned * sil.unit_price) AS total_return_value FROM sales_returns sr JOIN sales_return_lines srl ON sr.return_id = srl.return_id JOIN sales_invoice_lines sil ON sr.invoice_id = sil.invoice_id AND srl.item_no = sil.item_no GROUP BY sr.invoice_id ) sr ON si.invoice_id = sr.invoice_id WHERE si.status = 'Unpaid' GROUP BY c.customer_id, c.region ), average_ratios_by_region AS ( SELECT region, AVG(return_to_invoice_ratio) AS avg_ratio FROM total_sales_and_returns GROUP BY region ), unpaid_invoices AS ( SELECT customer_id, SUM(total_amount) AS total_unpaid FROM sales_invoices WHERE status = 'Unpaid' GROUP BY customer_id ) SELECT tsr.customer_id FROM total_sales_and_returns tsr JOIN average_ratios_by_region arbr ON tsr.region = arbr.region JOIN unpaid_invoices ui ON tsr.customer_id = ui.customer_id JOIN customer_credit_terms cct ON tsr.customer_id = cct.customer_id WHERE tsr.return_to_invoice_ratio > arbr.avg_ratio AND ui.total_unpaid >= 0.9 * cct.credit_limit;` — sr.total_return_value does not exist on table `sales_returns`
  2. `WITH total_sales_and_returns AS ( SELECT c.customer_id, c.region, COALESCE(SUM(srl.qty_returned * sil.unit_price) / NULLIF(SUM(si.total_amount), 0), 0) AS return_to_invoice_ratio FROM customers c JOIN sales_invoices si ON c.customer_id = si.customer_id LEFT JOIN sales_returns sr ON si.invoice_id = sr.invoice_id LEFT JOIN sales_return_lines srl ON sr.return_id = srl.return_id LEFT JOIN sales_invoice_lines sil ON si.invoice_id = sil.invoice_id AND sil.item_no = srl.item_no WHERE si.status = 'Unpaid' GROUP BY c.customer_id, c.region ), average_ratios_by_region AS ( SELECT region, AVG(return_to_invoice_ratio) AS avg_ratio FROM total_sales_and_returns GROUP BY region ), unpaid_invoices AS ( SELECT customer_id, SUM(total_amount) AS total_unpaid FROM sales_invoices WHERE status = 'Unpaid' GROUP BY customer_id ) SELECT tsr.customer_id FROM total_sales_and_returns tsr JOIN average_ratios_by_region arbr ON tsr.region = arbr.region JOIN unpaid_invoices ui ON tsr.customer_id = ui.customer_id JOIN customer_credit_terms cct ON tsr.customer_id = cct.customer_id WHERE tsr.return_to_invoice_ratio > arbr.avg_ratio AND ui.total_unpaid >= 0.9 * cct.credit_limit;` — si.customer_id does not exist on table `sales_invoices`
- Outcome after 1 correction attempt(s): fixed — `WITH total_sales_and_returns AS ( SELECT c.customer_id, c.region, COALESCE(SUM(srl.qty_returned * sil.unit_price) / NULLIF(SUM(si.total_amount), 0), 0) AS return_to_invoice_ratio FROM customers c JOIN sales_invoices si ON c.customer_id = si.customer_id LEFT JOIN sales_returns sr ON si.invoice_id = sr.invoice_id LEFT JOIN sales_return_lines srl ON sr.return_id = srl.return_id LEFT JOIN sales_invoice_lines sil ON si.invoice_id = sil.invoice_id AND sil.item_no = srl.item_no WHERE si.status = 'Unpaid' GROUP BY c.customer_id, c.region ), average_ratios_by_region AS ( SELECT region, AVG(return_to_invoice_ratio) AS avg_ratio FROM total_sales_and_returns GROUP BY region ), unpaid_invoices AS ( SELECT customer_id, SUM(total_amount) AS total_unpaid FROM sales_invoices WHERE status = 'Unpaid' GROUP BY customer_id ) SELECT tsr.customer_id FROM total_sales_and_returns tsr JOIN average_ratios_by_region arbr ON tsr.region = arbr.region JOIN unpaid_invoices ui ON tsr.customer_id = ui.customer_id JOIN customer_credit_terms cct ON tsr.customer_id = cct.customer_id WHERE tsr.return_to_invoice_ratio > arbr.avg_ratio AND ui.total_unpaid >= 0.9 * cct.credit_limit;`
- Suggested wiki fix: `entities/sales_returns` WAS in context but the model still invented a column/table — check whether its Column Reference Table, Common Query Examples, or Known Query Gotchas clearly distinguish similarly-named columns.

## [2026-08-03] purchase_requisitions — validation failed on first attempt

- Question: "For every PO, compute requisition→PO, PO→receipt, and receipt→invoice→payment lag in days, then find vendors whose combined cycle time is in the slowest 10% but whose on_time_rate in vendor_performance is still above 95%"
- Wiki pages used: entities/purchase_orders, entities/po_receipts, entities/vendor_invoices, entities/payment_transactions, entities/vendor_performance, relationships/purchase_orders-purchase_requisitions, relationships/po_receipts-purchase_orders, relationships/vendor_invoices-purchase_orders
- Rejected SQL: `WITH cycle_times AS ( SELECT po.vendor_id, po.po_id, po.po_date - pr.creation_date AS requisition_to_po, r.receipt_date - po.po_date AS po_to_receipt, vi.invoice_date - r.receipt_date AS receipt_to_invoice, pt.payment_date - vi.invoice_date AS invoice_to_payment, po.vendor_id FROM purchase_orders po JOIN purchase_requisitions pr ON po.requisition_id = pr.requisition_id JOIN po_receipts r ON po.po_id = r.po_id JOIN vendor_invoices vi ON po.po_id = vi.po_id JOIN payment_transactions pt ON vi.invoice_id = pt.vendor_invoice_id ), combined_cycle_times AS ( SELECT vendor_id, SUM(requisition_to_po + po_to_receipt + receipt_to_invoice + invoice_to_payment) AS total_cycle_time FROM cycle_times GROUP BY vendor_id ), slowest_vendors AS ( SELECT vendor_id FROM combined_cycle_times WHERE total_cycle_time > ( SELECT PERCENTILE_CONT(0.9) WITHIN GROUP (ORDER BY total_cycle_time) FROM combined_cycle_times ) ) SELECT vp.vendor_id FROM vendor_performance vp JOIN slowest_vendors sv ON vp.vendor_id = sv.vendor_id WHERE vp.on_time_rate > 95.00;`
- Validation error: pr.creation_date does not exist on table `purchase_requisitions`
- All rejected attempts:
  1. `WITH cycle_times AS ( SELECT po.vendor_id, po.po_id, po.po_date - pr.creation_date AS requisition_to_po, r.receipt_date - po.po_date AS po_to_receipt, vi.invoice_date - r.receipt_date AS receipt_to_invoice, pt.payment_date - vi.invoice_date AS invoice_to_payment, po.vendor_id FROM purchase_orders po JOIN purchase_requisitions pr ON po.requisition_id = pr.requisition_id JOIN po_receipts r ON po.po_id = r.po_id JOIN vendor_invoices vi ON po.po_id = vi.po_id JOIN payment_transactions pt ON vi.invoice_id = pt.vendor_invoice_id ), combined_cycle_times AS ( SELECT vendor_id, SUM(requisition_to_po + po_to_receipt + receipt_to_invoice + invoice_to_payment) AS total_cycle_time FROM cycle_times GROUP BY vendor_id ), slowest_vendors AS ( SELECT vendor_id FROM combined_cycle_times WHERE total_cycle_time > ( SELECT PERCENTILE_CONT(0.9) WITHIN GROUP (ORDER BY total_cycle_time) FROM combined_cycle_times ) ) SELECT vp.vendor_id FROM vendor_performance vp JOIN slowest_vendors sv ON vp.vendor_id = sv.vendor_id WHERE vp.on_time_rate > 95.00;` — pr.creation_date does not exist on table `purchase_requisitions`
- Outcome after 0 correction attempt(s): fixed — `WITH cycle_times AS ( SELECT po.vendor_id, po.po_id, po.po_date - pr.creation_date AS requisition_to_po, r.receipt_date - po.po_date AS po_to_receipt, vi.invoice_date - r.receipt_date AS receipt_to_invoice, pt.payment_date - vi.invoice_date AS invoice_to_payment, po.vendor_id FROM purchase_orders po JOIN purchase_requisitions pr ON po.requisition_id = pr.requisition_id JOIN po_receipts r ON po.po_id = r.po_id JOIN vendor_invoices vi ON po.po_id = vi.po_id JOIN payment_transactions pt ON vi.invoice_id = pt.vendor_invoice_id ), combined_cycle_times AS ( SELECT vendor_id, SUM(requisition_to_po + po_to_receipt + receipt_to_invoice + invoice_to_payment) AS total_cycle_time FROM cycle_times GROUP BY vendor_id ), slowest_vendors AS ( SELECT vendor_id FROM combined_cycle_times WHERE total_cycle_time > ( SELECT PERCENTILE_CONT(0.9) WITHIN GROUP (ORDER BY total_cycle_time) FROM combined_cycle_times ) ) SELECT vp.vendor_id FROM vendor_performance vp JOIN slowest_vendors sv ON vp.vendor_id = sv.vendor_id WHERE vp.on_time_rate > 95.00;`
- Suggested wiki fix: `entities/purchase_requisitions` was NOT among the pages used, so its real column list wasn't in context when the query was written — check whether pages linking to `purchase_requisitions` surface its key columns directly.

## [2026-08-03] purchase_order_lines — validation failed on first attempt

- Question: "For every growth_opportunity_event row, join to the actual downstream sales_order_lines and purchase_order_lines for that item_no/site/vendor combo to see if the flagged opportunity actually converted into higher sales volume within 60 days of the event, net of any sales_returns on those same items"
- Wiki pages used: entities/growth_opportunity_event, relationships/growth_opportunity_event-items, relationships/growth_opportunity_event-sites, relationships/growth_opportunity_event-vendors, entities/sales_order_lines, relationships/sales_order_lines-items, relationships/sales_order_lines-sales_orders, queries/sales-orders-shipments
- Rejected SQL: `WITH sales_within_60_days AS ( SELECT sol.item_no, sol.order_id, SUM(sol.qty_ordered) AS total_qty_ordered FROM growth_opportunity_event goe JOIN sales_order_lines sol ON goe.item_no = sol.item_no JOIN sales_orders so ON sol.order_id = so.order_id WHERE so.order_date BETWEEN goe.created_timestamp AND goe.created_timestamp + INTERVAL '60 days' AND goe.true_vendor = (SELECT vendor_id FROM vendors WHERE vendors.vendor_id = goe.true_vendor) AND goe.site = (SELECT site FROM sites WHERE sites.site = goe.site) GROUP BY sol.item_no, sol.order_id ), purchase_orders AS ( SELECT pol.item_no, pol.order_id, SUM(pol.qty_ordered) AS total_qty_ordered FROM growth_opportunity_event goe JOIN purchase_order_lines pol ON goe.item_no = pol.item_no JOIN purchase_orders po ON pol.order_id = po.order_id WHERE po.order_date BETWEEN goe.created_timestamp AND goe.created_timestamp + INTERVAL '60 days' AND goe.true_vendor = (SELECT vendor_id FROM vendors WHERE vendors.vendor_id = goe.true_vendor) AND goe.site = (SELECT site FROM sites WHERE sites.site = goe.site) GROUP BY pol.item_no, pol.order_id ), net_sales AS ( SELECT s.item_no, s.order_id, (s.total_qty_ordered - COALESCE(SUM(srl.qty_returned), 0)) AS net_qty_ordered FROM sales_within_60_days s LEFT JOIN sales_return_lines srl ON s.item_no = srl.item_no AND s.order_id = srl.order_id GROUP BY s.item_no, s.order_id ) SELECT n.item_no, n.net_qty_ordered, p.total_qty_ordered AS po_total_qty FROM net_sales n JOIN purchase_orders p ON n.item_no = p.item_no;`
- Validation error: pol.order_id does not exist on table `purchase_order_lines`
- All rejected attempts:
  1. `WITH sales_within_60_days AS ( SELECT sol.item_no, sol.order_id, SUM(sol.qty_ordered) AS total_qty_ordered FROM growth_opportunity_event goe JOIN sales_order_lines sol ON goe.item_no = sol.item_no JOIN sales_orders so ON sol.order_id = so.order_id WHERE so.order_date BETWEEN goe.created_timestamp AND goe.created_timestamp + INTERVAL '60 days' AND goe.true_vendor = (SELECT vendor_id FROM vendors WHERE vendors.vendor_id = goe.true_vendor) AND goe.site = (SELECT site FROM sites WHERE sites.site = goe.site) GROUP BY sol.item_no, sol.order_id ), purchase_orders AS ( SELECT pol.item_no, pol.order_id, SUM(pol.qty_ordered) AS total_qty_ordered FROM growth_opportunity_event goe JOIN purchase_order_lines pol ON goe.item_no = pol.item_no JOIN purchase_orders po ON pol.order_id = po.order_id WHERE po.order_date BETWEEN goe.created_timestamp AND goe.created_timestamp + INTERVAL '60 days' AND goe.true_vendor = (SELECT vendor_id FROM vendors WHERE vendors.vendor_id = goe.true_vendor) AND goe.site = (SELECT site FROM sites WHERE sites.site = goe.site) GROUP BY pol.item_no, pol.order_id ), net_sales AS ( SELECT s.item_no, s.order_id, (s.total_qty_ordered - COALESCE(SUM(srl.qty_returned), 0)) AS net_qty_ordered FROM sales_within_60_days s LEFT JOIN sales_return_lines srl ON s.item_no = srl.item_no AND s.order_id = srl.order_id GROUP BY s.item_no, s.order_id ) SELECT n.item_no, n.net_qty_ordered, p.total_qty_ordered AS po_total_qty FROM net_sales n JOIN purchase_orders p ON n.item_no = p.item_no;` — pol.order_id does not exist on table `purchase_order_lines`
  2. `WITH sales_within_60_days AS ( SELECT sol.item_no, so.order_id, SUM(sol.qty_ordered) AS total_qty_ordered FROM growth_opportunity_event goe JOIN sales_order_lines sol ON goe.item_no = sol.item_no JOIN sales_orders so ON sol.order_id = so.order_id WHERE so.order_date BETWEEN goe.created_timestamp AND goe.created_timestamp + INTERVAL '60 days' AND goe.true_vendor = (SELECT vendor_id FROM vendors WHERE vendors.vendor_id = goe.true_vendor) AND goe.site = (SELECT site FROM sites WHERE sites.site = goe.site) GROUP BY sol.item_no, so.order_id ), purchase_orders AS ( SELECT pol.item_no, po.po_id, SUM(pol.qty_ordered) AS total_qty_ordered FROM growth_opportunity_event goe JOIN purchase_order_lines pol ON goe.item_no = pol.item_no JOIN purchase_orders po ON pol.po_id = po.po_id WHERE po.order_date BETWEEN goe.created_timestamp AND goe.created_timestamp + INTERVAL '60 days' AND goe.true_vendor = (SELECT vendor_id FROM vendors WHERE vendors.vendor_id = goe.true_vendor) AND goe.site = (SELECT site FROM sites WHERE sites.site = goe.site) GROUP BY pol.item_no, po.po_id ), net_sales AS ( SELECT s.item_no, s.order_id, (s.total_qty_ordered - COALESCE(SUM(srl.qty_returned), 0)) AS net_qty_ordered FROM sales_within_60_days s LEFT JOIN sales_return_lines srl ON s.item_no = srl.item_no AND s.order_id = srl.order_id GROUP BY s.item_no, s.order_id ) SELECT n.item_no, n.net_qty_ordered, p.total_qty_ordered AS po_total_qty FROM net_sales n JOIN purchase_orders p ON n.item_no = p.item_no;` — po.order_date does not exist on table `purchase_orders`
- Outcome after 1 correction attempt(s): fixed — `WITH sales_within_60_days AS ( SELECT sol.item_no, so.order_id, SUM(sol.qty_ordered) AS total_qty_ordered FROM growth_opportunity_event goe JOIN sales_order_lines sol ON goe.item_no = sol.item_no JOIN sales_orders so ON sol.order_id = so.order_id WHERE so.order_date BETWEEN goe.created_timestamp AND goe.created_timestamp + INTERVAL '60 days' AND goe.true_vendor = (SELECT vendor_id FROM vendors WHERE vendors.vendor_id = goe.true_vendor) AND goe.site = (SELECT site FROM sites WHERE sites.site = goe.site) GROUP BY sol.item_no, so.order_id ), purchase_orders AS ( SELECT pol.item_no, po.po_id, SUM(pol.qty_ordered) AS total_qty_ordered FROM growth_opportunity_event goe JOIN purchase_order_lines pol ON goe.item_no = pol.item_no JOIN purchase_orders po ON pol.po_id = po.po_id WHERE po.order_date BETWEEN goe.created_timestamp AND goe.created_timestamp + INTERVAL '60 days' AND goe.true_vendor = (SELECT vendor_id FROM vendors WHERE vendors.vendor_id = goe.true_vendor) AND goe.site = (SELECT site FROM sites WHERE sites.site = goe.site) GROUP BY pol.item_no, po.po_id ), net_sales AS ( SELECT s.item_no, s.order_id, (s.total_qty_ordered - COALESCE(SUM(srl.qty_returned), 0)) AS net_qty_ordered FROM sales_within_60_days s LEFT JOIN sales_return_lines srl ON s.item_no = srl.item_no AND s.order_id = srl.order_id GROUP BY s.item_no, s.order_id ) SELECT n.item_no, n.net_qty_ordered, p.total_qty_ordered AS po_total_qty FROM net_sales n JOIN purchase_orders p ON n.item_no = p.item_no;`
- Suggested wiki fix: `entities/purchase_order_lines` was NOT among the pages used, so its real column list wasn't in context when the query was written — check whether pages linking to `purchase_order_lines` surface its key columns directly.

## [2026-08-03] purchase_orders — validation failed on first attempt

- Question: "For every growth_opportunity_event row, join to the actual downstream sales_order_lines and purchase_order_lines for that item_no/site/vendor combo to see if the flagged opportunity actually converted into higher sales volume within 60 days of the event, net of any sales_returns on those same items"
- Wiki pages used: entities/growth_opportunity_event, relationships/growth_opportunity_event-items, relationships/growth_opportunity_event-sites, relationships/growth_opportunity_event-vendors, entities/sales_order_lines, entities/purchase_order_lines, entities/sales_returns, relationships/sales_order_lines-sales_orders
- Rejected SQL: `WITH sales_within_60_days AS ( SELECT goe.event_id, sol.item_no, SUM(sol.qty_ordered) AS total_sales_qty FROM growth_opportunity_event goe JOIN sales_order_lines sol ON goe.item_no = sol.item_no JOIN sales_orders so ON sol.order_id = so.order_id WHERE so.order_date BETWEEN goe.created_timestamp AND goe.created_timestamp + INTERVAL '60 days' AND goe.site = (SELECT site FROM sites WHERE sites.site = goe.site) AND goe.true_vendor = (SELECT vendor_id FROM vendors WHERE vendors.vendor_id = goe.true_vendor) GROUP BY goe.event_id, sol.item_no ), purchase_orders_within_60_days AS ( SELECT goe.event_id, pol.item_no, SUM(pol.qty_ordered) AS total_purchase_qty FROM growth_opportunity_event goe JOIN purchase_order_lines pol ON goe.item_no = pol.item_no JOIN purchase_orders po ON pol.po_id = po.po_id WHERE po.order_date BETWEEN goe.created_timestamp AND goe.created_timestamp + INTERVAL '60 days' AND goe.site = (SELECT site FROM sites WHERE sites.site = goe.site) AND goe.true_vendor = (SELECT vendor_id FROM vendors WHERE vendors.vendor_id = goe.true_vendor) GROUP BY goe.event_id, pol.item_no ), net_sales AS ( SELECT s.event_id, s.item_no, (s.total_sales_qty - COALESCE(SUM(srl.qty_returned), 0)) AS net_sales_qty FROM sales_within_60_days s LEFT JOIN sales_returns sr ON sr.invoice_id = s.item_no LEFT JOIN sales_return_lines srl ON sr.return_id = srl.return_id AND s.item_no = srl.item_no GROUP BY s.event_id, s.item_no, s.total_sales_qty ) SELECT goe.event_id, n.item_no, n.net_sales_qty, p.total_purchase_qty FROM growth_opportunity_event goe JOIN net_sales n ON goe.event_id = n.event_id JOIN purchase_orders_within_60_days p ON n.item_no = p.item_no;`
- Validation error: po.order_date does not exist on table `purchase_orders`
- All rejected attempts:
  1. `WITH sales_within_60_days AS ( SELECT goe.event_id, sol.item_no, SUM(sol.qty_ordered) AS total_sales_qty FROM growth_opportunity_event goe JOIN sales_order_lines sol ON goe.item_no = sol.item_no JOIN sales_orders so ON sol.order_id = so.order_id WHERE so.order_date BETWEEN goe.created_timestamp AND goe.created_timestamp + INTERVAL '60 days' AND goe.site = (SELECT site FROM sites WHERE sites.site = goe.site) AND goe.true_vendor = (SELECT vendor_id FROM vendors WHERE vendors.vendor_id = goe.true_vendor) GROUP BY goe.event_id, sol.item_no ), purchase_orders_within_60_days AS ( SELECT goe.event_id, pol.item_no, SUM(pol.qty_ordered) AS total_purchase_qty FROM growth_opportunity_event goe JOIN purchase_order_lines pol ON goe.item_no = pol.item_no JOIN purchase_orders po ON pol.po_id = po.po_id WHERE po.order_date BETWEEN goe.created_timestamp AND goe.created_timestamp + INTERVAL '60 days' AND goe.site = (SELECT site FROM sites WHERE sites.site = goe.site) AND goe.true_vendor = (SELECT vendor_id FROM vendors WHERE vendors.vendor_id = goe.true_vendor) GROUP BY goe.event_id, pol.item_no ), net_sales AS ( SELECT s.event_id, s.item_no, (s.total_sales_qty - COALESCE(SUM(srl.qty_returned), 0)) AS net_sales_qty FROM sales_within_60_days s LEFT JOIN sales_returns sr ON sr.invoice_id = s.item_no LEFT JOIN sales_return_lines srl ON sr.return_id = srl.return_id AND s.item_no = srl.item_no GROUP BY s.event_id, s.item_no, s.total_sales_qty ) SELECT goe.event_id, n.item_no, n.net_sales_qty, p.total_purchase_qty FROM growth_opportunity_event goe JOIN net_sales n ON goe.event_id = n.event_id JOIN purchase_orders_within_60_days p ON n.item_no = p.item_no;` — po.order_date does not exist on table `purchase_orders`
- Outcome after 0 correction attempt(s): fixed — `WITH sales_within_60_days AS ( SELECT goe.event_id, sol.item_no, SUM(sol.qty_ordered) AS total_sales_qty FROM growth_opportunity_event goe JOIN sales_order_lines sol ON goe.item_no = sol.item_no JOIN sales_orders so ON sol.order_id = so.order_id WHERE so.order_date BETWEEN goe.created_timestamp AND goe.created_timestamp + INTERVAL '60 days' AND goe.site = (SELECT site FROM sites WHERE sites.site = goe.site) AND goe.true_vendor = (SELECT vendor_id FROM vendors WHERE vendors.vendor_id = goe.true_vendor) GROUP BY goe.event_id, sol.item_no ), purchase_orders_within_60_days AS ( SELECT goe.event_id, pol.item_no, SUM(pol.qty_ordered) AS total_purchase_qty FROM growth_opportunity_event goe JOIN purchase_order_lines pol ON goe.item_no = pol.item_no JOIN purchase_orders po ON pol.po_id = po.po_id WHERE po.order_date BETWEEN goe.created_timestamp AND goe.created_timestamp + INTERVAL '60 days' AND goe.site = (SELECT site FROM sites WHERE sites.site = goe.site) AND goe.true_vendor = (SELECT vendor_id FROM vendors WHERE vendors.vendor_id = goe.true_vendor) GROUP BY goe.event_id, pol.item_no ), net_sales AS ( SELECT s.event_id, s.item_no, (s.total_sales_qty - COALESCE(SUM(srl.qty_returned), 0)) AS net_sales_qty FROM sales_within_60_days s LEFT JOIN sales_returns sr ON sr.invoice_id = s.item_no LEFT JOIN sales_return_lines srl ON sr.return_id = srl.return_id AND s.item_no = srl.item_no GROUP BY s.event_id, s.item_no, s.total_sales_qty ) SELECT goe.event_id, n.item_no, n.net_sales_qty, p.total_purchase_qty FROM growth_opportunity_event goe JOIN net_sales n ON goe.event_id = n.event_id JOIN purchase_orders_within_60_days p ON n.item_no = p.item_no;`
- Suggested wiki fix: `entities/purchase_orders` was NOT among the pages used, so its real column list wasn't in context when the query was written — check whether pages linking to `purchase_orders` surface its key columns directly.

## [2026-08-03] sales_orders — validation failed on first attempt

- Question: "For every growth_opportunity_event row, join to the actual downstream sales_order_lines and purchase_order_lines for that item_no/site/vendor combo to see if the flagged opportunity actually converted into higher sales volume within 60 days of the event, net of any sales_returns on those same items"
- Wiki pages used: entities/growth_opportunity_event, entities/sales_order_lines, entities/purchase_order_lines, entities/sales_returns, relationships/growth_opportunity_event-items, relationships/growth_opportunity_event-sites, relationships/growth_opportunity_event-source_vendors, relationships/overview
- Rejected SQL: `WITH sales_within_60_days AS ( SELECT goe.event_id, sol.item_no, SUM(sol.qty_ordered) AS total_sales_qty FROM growth_opportunity_event goe JOIN sales_order_lines sol ON goe.item_no = sol.item_no JOIN sales_orders so ON sol.order_id = so.order_id WHERE so.date BETWEEN goe.created_timestamp AND goe.created_timestamp + INTERVAL '60 days' AND goe.true_vendor = (SELECT vendor_id FROM vendors WHERE vendors.vendor_id = goe.true_vendor) AND goe.site = (SELECT site FROM sites WHERE sites.site = goe.site) GROUP BY goe.event_id, sol.item_no ), purchase_orders_within_60_days AS ( SELECT goe.event_id, pol.item_no, SUM(pol.qty_ordered) AS total_purchase_qty FROM growth_opportunity_event goe JOIN purchase_order_lines pol ON goe.item_no = pol.item_no JOIN purchase_orders po ON pol.po_id = po.po_id WHERE po.date BETWEEN goe.created_timestamp AND goe.created_timestamp + INTERVAL '60 days' AND goe.true_vendor = (SELECT vendor_id FROM vendors WHERE vendors.vendor_id = goe.true_vendor) AND goe.site = (SELECT site FROM sites WHERE sites.site = goe.site) GROUP BY goe.event_id, pol.item_no ), net_sales AS ( SELECT s.event_id, s.item_no, (s.total_sales_qty - COALESCE(SUM(srl.qty_returned), 0)) AS net_sales_qty FROM sales_within_60_days s LEFT JOIN sales_returns sr ON sr.invoice_id = s.item_no LEFT JOIN sales_return_lines srl ON sr.return_id = srl.return_id AND s.item_no = srl.item_no GROUP BY s.event_id, s.item_no, s.total_sales_qty ) SELECT goe.event_id, n.item_no, n.net_sales_qty, p.total_purchase_qty FROM growth_opportunity_event goe JOIN net_sales n ON goe.event_id = n.event_id JOIN purchase_orders_within_60_days p ON n.item_no = p.item_no;`
- Validation error: so.date does not exist on table `sales_orders`
- All rejected attempts:
  1. `WITH sales_within_60_days AS ( SELECT goe.event_id, sol.item_no, SUM(sol.qty_ordered) AS total_sales_qty FROM growth_opportunity_event goe JOIN sales_order_lines sol ON goe.item_no = sol.item_no JOIN sales_orders so ON sol.order_id = so.order_id WHERE so.date BETWEEN goe.created_timestamp AND goe.created_timestamp + INTERVAL '60 days' AND goe.true_vendor = (SELECT vendor_id FROM vendors WHERE vendors.vendor_id = goe.true_vendor) AND goe.site = (SELECT site FROM sites WHERE sites.site = goe.site) GROUP BY goe.event_id, sol.item_no ), purchase_orders_within_60_days AS ( SELECT goe.event_id, pol.item_no, SUM(pol.qty_ordered) AS total_purchase_qty FROM growth_opportunity_event goe JOIN purchase_order_lines pol ON goe.item_no = pol.item_no JOIN purchase_orders po ON pol.po_id = po.po_id WHERE po.date BETWEEN goe.created_timestamp AND goe.created_timestamp + INTERVAL '60 days' AND goe.true_vendor = (SELECT vendor_id FROM vendors WHERE vendors.vendor_id = goe.true_vendor) AND goe.site = (SELECT site FROM sites WHERE sites.site = goe.site) GROUP BY goe.event_id, pol.item_no ), net_sales AS ( SELECT s.event_id, s.item_no, (s.total_sales_qty - COALESCE(SUM(srl.qty_returned), 0)) AS net_sales_qty FROM sales_within_60_days s LEFT JOIN sales_returns sr ON sr.invoice_id = s.item_no LEFT JOIN sales_return_lines srl ON sr.return_id = srl.return_id AND s.item_no = srl.item_no GROUP BY s.event_id, s.item_no, s.total_sales_qty ) SELECT goe.event_id, n.item_no, n.net_sales_qty, p.total_purchase_qty FROM growth_opportunity_event goe JOIN net_sales n ON goe.event_id = n.event_id JOIN purchase_orders_within_60_days p ON n.item_no = p.item_no;` — so.date does not exist on table `sales_orders`
  2. `WITH sales_within_60_days AS ( SELECT goe.event_id, sol.item_no, SUM(sol.qty_ordered) AS total_sales_qty FROM growth_opportunity_event goe JOIN sales_order_lines sol ON goe.item_no = sol.item_no JOIN sales_orders so ON sol.order_id = so.order_id WHERE so.order_date BETWEEN goe.created_timestamp AND goe.created_timestamp + INTERVAL '60 days' AND goe.true_vendor = (SELECT vendor_id FROM vendors WHERE vendors.vendor_id = goe.true_vendor) AND goe.site = (SELECT site FROM sites WHERE sites.site = goe.site) GROUP BY goe.event_id, sol.item_no ), purchase_orders_within_60_days AS ( SELECT goe.event_id, pol.item_no, SUM(pol.qty_ordered) AS total_purchase_qty FROM growth_opportunity_event goe JOIN purchase_order_lines pol ON goe.item_no = pol.item_no JOIN purchase_orders po ON pol.po_id = po.po_id WHERE po.order_date BETWEEN goe.created_timestamp AND goe.created_timestamp + INTERVAL '60 days' AND goe.true_vendor = (SELECT vendor_id FROM vendors WHERE vendors.vendor_id = goe.true_vendor) AND goe.site = (SELECT site FROM sites WHERE sites.site = goe.site) GROUP BY goe.event_id, pol.item_no ), net_sales AS ( SELECT s.event_id, s.item_no, (s.total_sales_qty - COALESCE(SUM(srl.qty_returned), 0)) AS net_sales_qty FROM sales_within_60_days s LEFT JOIN sales_returns sr ON sr.invoice_id = s.item_no LEFT JOIN sales_return_lines srl ON sr.return_id = srl.return_id AND s.item_no = srl.item_no GROUP BY s.event_id, s.item_no, s.total_sales_qty ) SELECT goe.event_id, n.item_no, n.net_sales_qty, p.total_purchase_qty FROM growth_opportunity_event goe JOIN net_sales n ON goe.event_id = n.event_id JOIN purchase_orders_within_60_days p ON n.item_no = p.item_no;` — po.order_date does not exist on table `purchase_orders`
- Outcome after 1 correction attempt(s): fixed — `WITH sales_within_60_days AS ( SELECT goe.event_id, sol.item_no, SUM(sol.qty_ordered) AS total_sales_qty FROM growth_opportunity_event goe JOIN sales_order_lines sol ON goe.item_no = sol.item_no JOIN sales_orders so ON sol.order_id = so.order_id WHERE so.order_date BETWEEN goe.created_timestamp AND goe.created_timestamp + INTERVAL '60 days' AND goe.true_vendor = (SELECT vendor_id FROM vendors WHERE vendors.vendor_id = goe.true_vendor) AND goe.site = (SELECT site FROM sites WHERE sites.site = goe.site) GROUP BY goe.event_id, sol.item_no ), purchase_orders_within_60_days AS ( SELECT goe.event_id, pol.item_no, SUM(pol.qty_ordered) AS total_purchase_qty FROM growth_opportunity_event goe JOIN purchase_order_lines pol ON goe.item_no = pol.item_no JOIN purchase_orders po ON pol.po_id = po.po_id WHERE po.order_date BETWEEN goe.created_timestamp AND goe.created_timestamp + INTERVAL '60 days' AND goe.true_vendor = (SELECT vendor_id FROM vendors WHERE vendors.vendor_id = goe.true_vendor) AND goe.site = (SELECT site FROM sites WHERE sites.site = goe.site) GROUP BY goe.event_id, pol.item_no ), net_sales AS ( SELECT s.event_id, s.item_no, (s.total_sales_qty - COALESCE(SUM(srl.qty_returned), 0)) AS net_sales_qty FROM sales_within_60_days s LEFT JOIN sales_returns sr ON sr.invoice_id = s.item_no LEFT JOIN sales_return_lines srl ON sr.return_id = srl.return_id AND s.item_no = srl.item_no GROUP BY s.event_id, s.item_no, s.total_sales_qty ) SELECT goe.event_id, n.item_no, n.net_sales_qty, p.total_purchase_qty FROM growth_opportunity_event goe JOIN net_sales n ON goe.event_id = n.event_id JOIN purchase_orders_within_60_days p ON n.item_no = p.item_no;`
- Suggested wiki fix: `entities/sales_orders` was NOT among the pages used, so its real column list wasn't in context when the query was written — check whether pages linking to `sales_orders` surface its key columns directly.

## [2026-08-03] purchase_orders — validation failed on first attempt

- Question: "Rank sites by the ratio of purchase dollars (purchase_order_lines × unit_cost) to sales dollars (sales_invoice_lines × unit_price) per month, and flag any site whose ratio moved by more than 2 standard deviations month-over-month.""
- Wiki pages used: entities/purchase_order_lines, entities/sales_invoice_lines, entities/sites, relationships/purchase_order_lines-purchase_orders, relationships/sales_invoice_lines-sales_invoices, relationships/overview, entities/purchase_requisition_lines, entities/source_vendors
- Rejected SQL: `WITH monthly_purchase AS ( SELECT po.po_id, po.site, DATE_TRUNC('month', po.created_timestamp) AS month, SUM(pol.qty_ordered * pol.unit_cost) AS purchase_dollars FROM purchase_orders po JOIN purchase_order_lines pol ON po.po_id = pol.po_id GROUP BY po.site, month ), monthly_sales AS ( SELECT si.site, DATE_TRUNC('month', si.created_timestamp) AS month, SUM(sil.qty_invoiced * sil.unit_price) AS sales_dollars FROM sales_invoices si JOIN sales_invoice_lines sil ON si.invoice_id = sil.invoice_id GROUP BY si.site, month ), monthly_ratios AS ( SELECT mp.site, mp.month, COALESCE(mp.purchase_dollars / ms.sales_dollars, 0) AS ratio FROM monthly_purchase mp LEFT JOIN monthly_sales ms ON mp.site = ms.site AND mp.month = ms.month ), stats AS ( SELECT site, month, ratio, AVG(ratio) OVER (PARTITION BY site ORDER BY month ROWS BETWEEN 1 PRECEDING AND 1 FOLLOWING) AS avg_next_prev, STDDEV(ratio) OVER (PARTITION BY site ORDER BY month ROWS BETWEEN 1 PRECEDING AND 1 FOLLOWING) AS stddev_next_prev FROM monthly_ratios ) SELECT s.site, s.month, s.ratio, s.avg_next_prev, s.stddev_next_prev, CASE WHEN ABS(s.ratio - LAG(s.ratio, 1) OVER (PARTITION BY s.site ORDER BY s.month)) > 2 * s.stddev_next_prev THEN 'Flagged' ELSE 'Normal' END as status FROM stats s ORDER BY s.site, s.month;`
- Validation error: po.created_timestamp does not exist on table `purchase_orders`
- All rejected attempts:
  1. `WITH monthly_purchase AS ( SELECT po.po_id, po.site, DATE_TRUNC('month', po.created_timestamp) AS month, SUM(pol.qty_ordered * pol.unit_cost) AS purchase_dollars FROM purchase_orders po JOIN purchase_order_lines pol ON po.po_id = pol.po_id GROUP BY po.site, month ), monthly_sales AS ( SELECT si.site, DATE_TRUNC('month', si.created_timestamp) AS month, SUM(sil.qty_invoiced * sil.unit_price) AS sales_dollars FROM sales_invoices si JOIN sales_invoice_lines sil ON si.invoice_id = sil.invoice_id GROUP BY si.site, month ), monthly_ratios AS ( SELECT mp.site, mp.month, COALESCE(mp.purchase_dollars / ms.sales_dollars, 0) AS ratio FROM monthly_purchase mp LEFT JOIN monthly_sales ms ON mp.site = ms.site AND mp.month = ms.month ), stats AS ( SELECT site, month, ratio, AVG(ratio) OVER (PARTITION BY site ORDER BY month ROWS BETWEEN 1 PRECEDING AND 1 FOLLOWING) AS avg_next_prev, STDDEV(ratio) OVER (PARTITION BY site ORDER BY month ROWS BETWEEN 1 PRECEDING AND 1 FOLLOWING) AS stddev_next_prev FROM monthly_ratios ) SELECT s.site, s.month, s.ratio, s.avg_next_prev, s.stddev_next_prev, CASE WHEN ABS(s.ratio - LAG(s.ratio, 1) OVER (PARTITION BY s.site ORDER BY s.month)) > 2 * s.stddev_next_prev THEN 'Flagged' ELSE 'Normal' END as status FROM stats s ORDER BY s.site, s.month;` — po.created_timestamp does not exist on table `purchase_orders`
  2. `WITH monthly_purchase AS ( SELECT po.site, DATE_TRUNC('month', po.po_date) AS month, SUM(pol.qty_ordered * pol.unit_cost) AS purchase_dollars FROM purchase_orders po JOIN purchase_order_lines pol ON po.po_id = pol.po_id GROUP BY po.site, month ), monthly_sales AS ( SELECT si.site, DATE_TRUNC('month', si.invoice_date) AS month, SUM(sil.qty_invoiced * sil.unit_price) AS sales_dollars FROM sales_invoices si JOIN sales_invoice_lines sil ON si.invoice_id = sil.invoice_id GROUP BY si.site, month ), monthly_ratios AS ( SELECT mp.site, mp.month, COALESCE(mp.purchase_dollars / ms.sales_dollars, 0) AS ratio FROM monthly_purchase mp LEFT JOIN monthly_sales ms ON mp.site = ms.site AND mp.month = ms.month ), stats AS ( SELECT site, month, ratio, AVG(ratio) OVER (PARTITION BY site ORDER BY month ROWS BETWEEN 1 PRECEDING AND 1 FOLLOWING) AS avg_next_prev, STDDEV(ratio) OVER (PARTITION BY site ORDER BY month ROWS BETWEEN 1 PRECEDING AND 1 FOLLOWING) AS stddev_next_prev FROM monthly_ratios ) SELECT s.site, s.month, s.ratio, s.avg_next_prev, s.stddev_next_prev, CASE WHEN ABS(s.ratio - LAG(s.ratio, 1) OVER (PARTITION BY s.site ORDER BY s.month)) > 2 * s.stddev_next_prev THEN 'Flagged' ELSE 'Normal' END as status FROM stats s ORDER BY s.site, s.month;` — si.site does not exist on table `sales_invoices`
  3. `WITH monthly_purchase AS ( SELECT po.site, DATE_TRUNC('month', po.po_date) AS month, SUM(pol.qty_ordered * pol.unit_cost) AS purchase_dollars FROM purchase_orders po JOIN purchase_order_lines pol ON po.po_id = pol.po_id GROUP BY po.site, month ), monthly_sales AS ( SELECT si.site, DATE_TRUNC('month', si.invoice_date) AS month, SUM(sil.qty_invoiced * sil.unit_price) AS sales_dollars FROM sales_invoices si JOIN sales_invoice_lines sil ON si.invoice_id = sil.invoice_id JOIN sales_orders so ON si.order_id = so.order_id GROUP BY si.site, month ), monthly_ratios AS ( SELECT mp.site, mp.month, COALESCE(mp.purchase_dollars / ms.sales_dollars, 0) AS ratio FROM monthly_purchase mp LEFT JOIN monthly_sales ms ON mp.site = ms.site AND mp.month = ms.month ), stats AS ( SELECT site, month, ratio, AVG(ratio) OVER (PARTITION BY site ORDER BY month ROWS BETWEEN 1 PRECEDING AND 1 FOLLOWING) AS avg_next_prev, STDDEV(ratio) OVER (PARTITION BY site ORDER BY month ROWS BETWEEN 1 PRECEDING AND 1 FOLLOWING) AS stddev_next_prev FROM monthly_ratios ) SELECT s.site, s.month, s.ratio, s.avg_next_prev, s.stddev_next_prev, CASE WHEN ABS(s.ratio - LAG(s.ratio, 1) OVER (PARTITION BY s.site ORDER BY s.month)) > 2 * s.stddev_next_prev THEN 'Flagged' ELSE 'Normal' END as status FROM stats s ORDER BY s.site, s.month;` — si.site does not exist on table `sales_invoices`
- Outcome after 2 correction attempt(s): STILL BROKEN — `WITH monthly_purchase AS ( SELECT po.site, DATE_TRUNC('month', po.po_date) AS month, SUM(pol.qty_ordered * pol.unit_cost) AS purchase_dollars FROM purchase_orders po JOIN purchase_order_lines pol ON po.po_id = pol.po_id GROUP BY po.site, month ), monthly_sales AS ( SELECT si.site, DATE_TRUNC('month', si.invoice_date) AS month, SUM(sil.qty_invoiced * sil.unit_price) AS sales_dollars FROM sales_invoices si JOIN sales_invoice_lines sil ON si.invoice_id = sil.invoice_id JOIN sales_orders so ON si.order_id = so.order_id GROUP BY si.site, month ), monthly_ratios AS ( SELECT mp.site, mp.month, COALESCE(mp.purchase_dollars / ms.sales_dollars, 0) AS ratio FROM monthly_purchase mp LEFT JOIN monthly_sales ms ON mp.site = ms.site AND mp.month = ms.month ), stats AS ( SELECT site, month, ratio, AVG(ratio) OVER (PARTITION BY site ORDER BY month ROWS BETWEEN 1 PRECEDING AND 1 FOLLOWING) AS avg_next_prev, STDDEV(ratio) OVER (PARTITION BY site ORDER BY month ROWS BETWEEN 1 PRECEDING AND 1 FOLLOWING) AS stddev_next_prev FROM monthly_ratios ) SELECT s.site, s.month, s.ratio, s.avg_next_prev, s.stddev_next_prev, CASE WHEN ABS(s.ratio - LAG(s.ratio, 1) OVER (PARTITION BY s.site ORDER BY s.month)) > 2 * s.stddev_next_prev THEN 'Flagged' ELSE 'Normal' END as status FROM stats s ORDER BY s.site, s.month;`
- Suggested wiki fix: `entities/purchase_orders` was NOT among the pages used, so its real column list wasn't in context when the query was written — check whether pages linking to `purchase_orders` surface its key columns directly.

## [2026-08-03] purchase_orders — validation failed on first attempt

- Question: "Rank sites by the ratio of purchase dollars (purchase_order_lines × unit_cost) to sales dollars (sales_invoice_lines × unit_price) per month, and flag any site whose ratio moved by more than 2 standard deviations month-over-month.""
- Wiki pages used: entities/purchase_order_lines, entities/sales_invoice_lines, entities/sites, relationships/purchase_orders-sites, relationships/sales_orders-sites, relationships/overview, entities/purchase_requisition_lines, entities/source_vendors
- Rejected SQL: `WITH purchases AS ( SELECT po.site, DATE_TRUNC('month', po.order_date) AS month, SUM(pol.qty_ordered * pol.unit_cost) AS purchase_dollars FROM purchase_order_lines pol JOIN purchase_orders po ON pol.po_id = po.po_id GROUP BY po.site, DATE_TRUNC('month', po.order_date) ), sales AS ( SELECT so.site, DATE_TRUNC('month', si.invoice_date) AS month, SUM(sil.qty_invoiced * sil.unit_price) AS sales_dollars FROM sales_invoice_lines sil JOIN sales_invoices si ON sil.invoice_id = si.invoice_id JOIN sales_orders so ON si.order_id = so.order_id GROUP BY so.site, DATE_TRUNC('month', si.invoice_date) ), ratio_analysis AS ( SELECT p.site, p.month, COALESCE(p.purchase_dollars / NULLIF(s.sales_dollars, 0), 0) AS purchase_to_sales_ratio FROM purchases p LEFT JOIN sales s ON p.site = s.site AND p.month = s.month ), ratio_deviation AS ( SELECT site, month, purchase_to_sales_ratio, AVG(purchase_to_sales_ratio) OVER (PARTITION BY site ORDER BY month ROWS BETWEEN 1 PRECEDING AND 1 FOLLOWING) AS avg_ratio, STDDEV(purchase_to_sales_ratio) OVER (PARTITION BY site ORDER BY month ROWS BETWEEN 1 PRECEDING AND 1 FOLLOWING) AS stddev_ratio FROM ratio_analysis ) SELECT site, month, purchase_to_sales_ratio, CASE WHEN ABS(purchase_to_sales_ratio - avg_ratio) > 2 * stddev_ratio THEN 'Flagged' ELSE 'Normal' END AS flag FROM ratio_deviation;`
- Validation error: po.order_date does not exist on table `purchase_orders`
- All rejected attempts:
  1. `WITH purchases AS ( SELECT po.site, DATE_TRUNC('month', po.order_date) AS month, SUM(pol.qty_ordered * pol.unit_cost) AS purchase_dollars FROM purchase_order_lines pol JOIN purchase_orders po ON pol.po_id = po.po_id GROUP BY po.site, DATE_TRUNC('month', po.order_date) ), sales AS ( SELECT so.site, DATE_TRUNC('month', si.invoice_date) AS month, SUM(sil.qty_invoiced * sil.unit_price) AS sales_dollars FROM sales_invoice_lines sil JOIN sales_invoices si ON sil.invoice_id = si.invoice_id JOIN sales_orders so ON si.order_id = so.order_id GROUP BY so.site, DATE_TRUNC('month', si.invoice_date) ), ratio_analysis AS ( SELECT p.site, p.month, COALESCE(p.purchase_dollars / NULLIF(s.sales_dollars, 0), 0) AS purchase_to_sales_ratio FROM purchases p LEFT JOIN sales s ON p.site = s.site AND p.month = s.month ), ratio_deviation AS ( SELECT site, month, purchase_to_sales_ratio, AVG(purchase_to_sales_ratio) OVER (PARTITION BY site ORDER BY month ROWS BETWEEN 1 PRECEDING AND 1 FOLLOWING) AS avg_ratio, STDDEV(purchase_to_sales_ratio) OVER (PARTITION BY site ORDER BY month ROWS BETWEEN 1 PRECEDING AND 1 FOLLOWING) AS stddev_ratio FROM ratio_analysis ) SELECT site, month, purchase_to_sales_ratio, CASE WHEN ABS(purchase_to_sales_ratio - avg_ratio) > 2 * stddev_ratio THEN 'Flagged' ELSE 'Normal' END AS flag FROM ratio_deviation;` — po.order_date does not exist on table `purchase_orders`
- Outcome after 0 correction attempt(s): fixed — `WITH purchases AS ( SELECT po.site, DATE_TRUNC('month', po.order_date) AS month, SUM(pol.qty_ordered * pol.unit_cost) AS purchase_dollars FROM purchase_order_lines pol JOIN purchase_orders po ON pol.po_id = po.po_id GROUP BY po.site, DATE_TRUNC('month', po.order_date) ), sales AS ( SELECT so.site, DATE_TRUNC('month', si.invoice_date) AS month, SUM(sil.qty_invoiced * sil.unit_price) AS sales_dollars FROM sales_invoice_lines sil JOIN sales_invoices si ON sil.invoice_id = si.invoice_id JOIN sales_orders so ON si.order_id = so.order_id GROUP BY so.site, DATE_TRUNC('month', si.invoice_date) ), ratio_analysis AS ( SELECT p.site, p.month, COALESCE(p.purchase_dollars / NULLIF(s.sales_dollars, 0), 0) AS purchase_to_sales_ratio FROM purchases p LEFT JOIN sales s ON p.site = s.site AND p.month = s.month ), ratio_deviation AS ( SELECT site, month, purchase_to_sales_ratio, AVG(purchase_to_sales_ratio) OVER (PARTITION BY site ORDER BY month ROWS BETWEEN 1 PRECEDING AND 1 FOLLOWING) AS avg_ratio, STDDEV(purchase_to_sales_ratio) OVER (PARTITION BY site ORDER BY month ROWS BETWEEN 1 PRECEDING AND 1 FOLLOWING) AS stddev_ratio FROM ratio_analysis ) SELECT site, month, purchase_to_sales_ratio, CASE WHEN ABS(purchase_to_sales_ratio - avg_ratio) > 2 * stddev_ratio THEN 'Flagged' ELSE 'Normal' END AS flag FROM ratio_deviation;`
- Suggested wiki fix: `entities/purchase_orders` was NOT among the pages used, so its real column list wasn't in context when the query was written — check whether pages linking to `purchase_orders` surface its key columns directly.

## [2026-08-03] purchase_orders — validation failed on first attempt

- Question: "Rank sites by the ratio of purchase dollars (purchase_order_lines × unit_cost) to sales dollars (sales_invoice_lines × unit_price) per month, and flag any site whose ratio moved by more than 2 standard deviations month-over-month.""
- Wiki pages used: entities/purchase_order_lines, entities/sales_invoice_lines, entities/sites, queries/purchase-orders-assoc-invoices, queries/sales-orders-shipments, relationships/overview, entities/purchase_requisition_lines, entities/source_vendors
- Rejected SQL: `WITH monthly_totals AS ( SELECT s.site, DATE_TRUNC('month', po.order_date) AS month, SUM(pol.qty_ordered * pol.unit_cost) AS purchase_dollars, SUM(sil.qty_invoiced * sil.unit_price) AS sales_dollars FROM purchase_order_lines pol JOIN purchase_orders po ON pol.po_id = po.po_id JOIN sites s ON po.site = s.site JOIN sales_invoice_lines sil ON sil.invoice_id = po.po_id JOIN sales_invoices si ON sil.invoice_id = si.invoice_id WHERE po.order_date IS NOT NULL AND si.invoice_date IS NOT NULL GROUP BY s.site, DATE_TRUNC('month', po.order_date) ), ratio_calculations AS ( SELECT site, month, CASE WHEN sales_dollars = 0 THEN NULL ELSE purchase_dollars / sales_dollars END AS month_ratio FROM monthly_totals ), month_deviations AS ( SELECT site, month, month_ratio, LAG(month_ratio) OVER (PARTITION BY site ORDER BY month) AS prev_month_ratio FROM ratio_calculations ), std_dev_calculation AS ( SELECT site, STDDEV(month_ratio) OVER (PARTITION BY site ORDER BY month) AS stddev_ratio FROM ratio_calculations ), flagged_sites AS ( SELECT md.site, md.month, md.month_ratio, CASE WHEN abs(md.month_ratio - md.prev_month_ratio) > 2 * st.stddev_ratio THEN 'FLAG' ELSE 'OK' END AS deviation_flag FROM month_deviations md JOIN std_dev_calculation st ON md.site = st.site ) SELECT site, month, month_ratio, deviation_flag FROM flagged_sites ORDER BY site, month;`
- Validation error: po.order_date does not exist on table `purchase_orders`
- All rejected attempts:
  1. `WITH monthly_totals AS ( SELECT s.site, DATE_TRUNC('month', po.order_date) AS month, SUM(pol.qty_ordered * pol.unit_cost) AS purchase_dollars, SUM(sil.qty_invoiced * sil.unit_price) AS sales_dollars FROM purchase_order_lines pol JOIN purchase_orders po ON pol.po_id = po.po_id JOIN sites s ON po.site = s.site JOIN sales_invoice_lines sil ON sil.invoice_id = po.po_id JOIN sales_invoices si ON sil.invoice_id = si.invoice_id WHERE po.order_date IS NOT NULL AND si.invoice_date IS NOT NULL GROUP BY s.site, DATE_TRUNC('month', po.order_date) ), ratio_calculations AS ( SELECT site, month, CASE WHEN sales_dollars = 0 THEN NULL ELSE purchase_dollars / sales_dollars END AS month_ratio FROM monthly_totals ), month_deviations AS ( SELECT site, month, month_ratio, LAG(month_ratio) OVER (PARTITION BY site ORDER BY month) AS prev_month_ratio FROM ratio_calculations ), std_dev_calculation AS ( SELECT site, STDDEV(month_ratio) OVER (PARTITION BY site ORDER BY month) AS stddev_ratio FROM ratio_calculations ), flagged_sites AS ( SELECT md.site, md.month, md.month_ratio, CASE WHEN abs(md.month_ratio - md.prev_month_ratio) > 2 * st.stddev_ratio THEN 'FLAG' ELSE 'OK' END AS deviation_flag FROM month_deviations md JOIN std_dev_calculation st ON md.site = st.site ) SELECT site, month, month_ratio, deviation_flag FROM flagged_sites ORDER BY site, month;` — po.order_date does not exist on table `purchase_orders`
- Outcome after 0 correction attempt(s): fixed — `WITH monthly_totals AS ( SELECT s.site, DATE_TRUNC('month', po.order_date) AS month, SUM(pol.qty_ordered * pol.unit_cost) AS purchase_dollars, SUM(sil.qty_invoiced * sil.unit_price) AS sales_dollars FROM purchase_order_lines pol JOIN purchase_orders po ON pol.po_id = po.po_id JOIN sites s ON po.site = s.site JOIN sales_invoice_lines sil ON sil.invoice_id = po.po_id JOIN sales_invoices si ON sil.invoice_id = si.invoice_id WHERE po.order_date IS NOT NULL AND si.invoice_date IS NOT NULL GROUP BY s.site, DATE_TRUNC('month', po.order_date) ), ratio_calculations AS ( SELECT site, month, CASE WHEN sales_dollars = 0 THEN NULL ELSE purchase_dollars / sales_dollars END AS month_ratio FROM monthly_totals ), month_deviations AS ( SELECT site, month, month_ratio, LAG(month_ratio) OVER (PARTITION BY site ORDER BY month) AS prev_month_ratio FROM ratio_calculations ), std_dev_calculation AS ( SELECT site, STDDEV(month_ratio) OVER (PARTITION BY site ORDER BY month) AS stddev_ratio FROM ratio_calculations ), flagged_sites AS ( SELECT md.site, md.month, md.month_ratio, CASE WHEN abs(md.month_ratio - md.prev_month_ratio) > 2 * st.stddev_ratio THEN 'FLAG' ELSE 'OK' END AS deviation_flag FROM month_deviations md JOIN std_dev_calculation st ON md.site = st.site ) SELECT site, month, month_ratio, deviation_flag FROM flagged_sites ORDER BY site, month;`
- Suggested wiki fix: `entities/purchase_orders` was NOT among the pages used, so its real column list wasn't in context when the query was written — check whether pages linking to `purchase_orders` surface its key columns directly.

## [2026-08-03] vendor_invoice_lines — validation failed on first attempt

- Question: "Compute, per vendor who is also effectively a cost driver for customers buying the same items (via shared item_no), the net cash position: sum of unpaid vendor_invoices owed to them minus a proportional share of unpaid sales_invoices on items they supply."
- Wiki pages used: entities/vendor_invoices, entities/sales_invoices, entities/vendors, entities/items, relationships/vendor_invoices-vendors, relationships/vendor_invoice_lines-items, relationships/sales_invoice_lines-items, relationships/vendor_invoices-purchase_orders
- Rejected SQL: `WITH vendor_unpaid AS ( SELECT vi.vendor_id, SUM(vi.total_amount) AS total_unpaid_vendor FROM vendor_invoices vi WHERE vi.status = 'Unpaid' GROUP BY vi.vendor_id ), sales_unpaid_per_item AS ( SELECT sil.item_no, SUM(si.total_amount) AS total_unpaid_sales FROM sales_invoices si JOIN sales_invoice_lines sil ON si.invoice_id = sil.invoice_id WHERE si.status = 'Unpaid' GROUP BY sil.item_no ), proportional_sales_unpaid AS ( SELECT vil.vendor_id, SUM(supi.total_unpaid_sales) AS total_shares_unpaid_sales FROM vendor_invoice_lines vil JOIN sales_unpaid_per_item supi ON vil.item_no = supi.item_no JOIN items i ON vil.item_no = i.item_no GROUP BY vil.vendor_id ) SELECT v.vendor_id, v.vendor_name, vup.total_unpaid_vendor - COALESCE(psu.total_shares_unpaid_sales, 0) AS net_cash_position FROM vendor_unpaid vup JOIN vendors v ON vup.vendor_id = v.vendor_id LEFT JOIN proportional_sales_unpaid psu ON vup.vendor_id = psu.vendor_id;`
- Validation error: vil.vendor_id does not exist on table `vendor_invoice_lines`
- All rejected attempts:
  1. `WITH vendor_unpaid AS ( SELECT vi.vendor_id, SUM(vi.total_amount) AS total_unpaid_vendor FROM vendor_invoices vi WHERE vi.status = 'Unpaid' GROUP BY vi.vendor_id ), sales_unpaid_per_item AS ( SELECT sil.item_no, SUM(si.total_amount) AS total_unpaid_sales FROM sales_invoices si JOIN sales_invoice_lines sil ON si.invoice_id = sil.invoice_id WHERE si.status = 'Unpaid' GROUP BY sil.item_no ), proportional_sales_unpaid AS ( SELECT vil.vendor_id, SUM(supi.total_unpaid_sales) AS total_shares_unpaid_sales FROM vendor_invoice_lines vil JOIN sales_unpaid_per_item supi ON vil.item_no = supi.item_no JOIN items i ON vil.item_no = i.item_no GROUP BY vil.vendor_id ) SELECT v.vendor_id, v.vendor_name, vup.total_unpaid_vendor - COALESCE(psu.total_shares_unpaid_sales, 0) AS net_cash_position FROM vendor_unpaid vup JOIN vendors v ON vup.vendor_id = v.vendor_id LEFT JOIN proportional_sales_unpaid psu ON vup.vendor_id = psu.vendor_id;` — vil.vendor_id does not exist on table `vendor_invoice_lines`
- Outcome after 0 correction attempt(s): fixed — `WITH vendor_unpaid AS ( SELECT vi.vendor_id, SUM(vi.total_amount) AS total_unpaid_vendor FROM vendor_invoices vi WHERE vi.status = 'Unpaid' GROUP BY vi.vendor_id ), sales_unpaid_per_item AS ( SELECT sil.item_no, SUM(si.total_amount) AS total_unpaid_sales FROM sales_invoices si JOIN sales_invoice_lines sil ON si.invoice_id = sil.invoice_id WHERE si.status = 'Unpaid' GROUP BY sil.item_no ), proportional_sales_unpaid AS ( SELECT vil.vendor_id, SUM(supi.total_unpaid_sales) AS total_shares_unpaid_sales FROM vendor_invoice_lines vil JOIN sales_unpaid_per_item supi ON vil.item_no = supi.item_no JOIN items i ON vil.item_no = i.item_no GROUP BY vil.vendor_id ) SELECT v.vendor_id, v.vendor_name, vup.total_unpaid_vendor - COALESCE(psu.total_shares_unpaid_sales, 0) AS net_cash_position FROM vendor_unpaid vup JOIN vendors v ON vup.vendor_id = v.vendor_id LEFT JOIN proportional_sales_unpaid psu ON vup.vendor_id = psu.vendor_id;`
- Suggested wiki fix: `entities/vendor_invoice_lines` was NOT among the pages used, so its real column list wasn't in context when the query was written — check whether pages linking to `vendor_invoice_lines` surface its key columns directly.

## [2026-08-03] purchase_orders — validation failed on first attempt

- Question: "Which sites have both a site_vendor_assignments primary vendor AND active sales_orders, and what's the gap between what they buy (PO $) and what they sell (invoice $) in the same month?"
- Wiki pages used: entities/sites, entities/site_vendor_assignments, entities/sales_orders, queries/site-vendor-assignments-primary-status, queries/customer-sales-orders, queries/purchase-orders-assoc-invoices, relationships/overview, entities/source_vendor_pricing
- Rejected SQL: `WITH po_totals AS ( SELECT po.site, EXTRACT(YEAR FROM po.po_date) AS year, EXTRACT(MONTH FROM po.po_date) AS month, SUM(po.total_amount) AS total_po_amount FROM purchase_orders po GROUP BY po.site, year, month ), invoice_totals AS ( SELECT si.site, EXTRACT(YEAR FROM si.invoice_date) AS year, EXTRACT(MONTH FROM si.invoice_date) AS month, SUM(si.total_amount) AS total_invoice_amount FROM sales_invoices si JOIN sales_orders so ON si.order_id = so.order_id WHERE so.status = 'Open' GROUP BY si.site, year, month ) SELECT sva.site, COALESCE(pt.total_po_amount, 0) AS total_po_amount, COALESCE(it.total_invoice_amount, 0) AS total_invoice_amount, (COALESCE(it.total_invoice_amount, 0) - COALESCE(pt.total_po_amount, 0)) AS gap FROM site_vendor_assignments sva JOIN invoice_totals it ON sva.site = it.site LEFT JOIN po_totals pt ON sva.site = pt.site AND it.year = pt.year AND it.month = pt.month WHERE sva.is_primary = 'Y' AND EXISTS ( SELECT 1 FROM sales_orders so WHERE so.site = sva.site AND so.status = 'Open' ) AND (pt.year IS NOT NULL OR it.year IS NOT NULL);`
- Validation error: po.total_amount does not exist on table `purchase_orders`
- All rejected attempts:
  1. `WITH po_totals AS ( SELECT po.site, EXTRACT(YEAR FROM po.po_date) AS year, EXTRACT(MONTH FROM po.po_date) AS month, SUM(po.total_amount) AS total_po_amount FROM purchase_orders po GROUP BY po.site, year, month ), invoice_totals AS ( SELECT si.site, EXTRACT(YEAR FROM si.invoice_date) AS year, EXTRACT(MONTH FROM si.invoice_date) AS month, SUM(si.total_amount) AS total_invoice_amount FROM sales_invoices si JOIN sales_orders so ON si.order_id = so.order_id WHERE so.status = 'Open' GROUP BY si.site, year, month ) SELECT sva.site, COALESCE(pt.total_po_amount, 0) AS total_po_amount, COALESCE(it.total_invoice_amount, 0) AS total_invoice_amount, (COALESCE(it.total_invoice_amount, 0) - COALESCE(pt.total_po_amount, 0)) AS gap FROM site_vendor_assignments sva JOIN invoice_totals it ON sva.site = it.site LEFT JOIN po_totals pt ON sva.site = pt.site AND it.year = pt.year AND it.month = pt.month WHERE sva.is_primary = 'Y' AND EXISTS ( SELECT 1 FROM sales_orders so WHERE so.site = sva.site AND so.status = 'Open' ) AND (pt.year IS NOT NULL OR it.year IS NOT NULL);` — po.total_amount does not exist on table `purchase_orders`
  2. `WITH po_totals AS ( SELECT po.site, EXTRACT(YEAR FROM po.po_date) AS year, EXTRACT(MONTH FROM po.po_date) AS month, SUM(pol.unit_price * pol.quantity) AS total_po_amount FROM purchase_orders po JOIN purchase_order_lines pol ON po.po_id = pol.po_id GROUP BY po.site, year, month ), invoice_totals AS ( SELECT so.site, EXTRACT(YEAR FROM si.invoice_date) AS year, EXTRACT(MONTH FROM si.invoice_date) AS month, SUM(sil.unit_price * sil.quantity) AS total_invoice_amount FROM sales_invoices si JOIN sales_orders so ON si.order_id = so.order_id JOIN sales_invoice_lines sil ON si.invoice_id = sil.invoice_id WHERE so.status = 'Open' GROUP BY so.site, year, month ) SELECT sva.site, COALESCE(pt.total_po_amount, 0) AS total_po_amount, COALESCE(it.total_invoice_amount, 0) AS total_invoice_amount, (COALESCE(it.total_invoice_amount, 0) - COALESCE(pt.total_po_amount, 0)) AS gap FROM site_vendor_assignments sva JOIN invoice_totals it ON sva.site = it.site LEFT JOIN po_totals pt ON sva.site = pt.site AND it.year = pt.year AND it.month = pt.month WHERE sva.is_primary = 'Y' AND EXISTS ( SELECT 1 FROM sales_orders so WHERE so.site = sva.site AND so.status = 'Open' ) AND (pt.year IS NOT NULL OR it.year IS NOT NULL);` — pol.unit_price does not exist on table `purchase_order_lines`
  3. `WITH po_totals AS ( SELECT po.site, EXTRACT(YEAR FROM po.po_date) AS year, EXTRACT(MONTH FROM po.po_date) AS month, SUM(pol.qty_ordered * pol.unit_cost) AS total_po_amount FROM purchase_orders po JOIN purchase_order_lines pol ON po.po_id = pol.po_id GROUP BY po.site, year, month ), invoice_totals AS ( SELECT so.site, EXTRACT(YEAR FROM si.invoice_date) AS year, EXTRACT(MONTH FROM si.invoice_date) AS month, SUM(sil.unit_price * sil.quantity) AS total_invoice_amount FROM sales_invoices si JOIN sales_invoice_lines sil ON si.invoice_id = sil.invoice_id JOIN sales_orders so ON si.order_id = so.order_id WHERE so.status = 'Open' GROUP BY so.site, year, month ) SELECT sva.site, COALESCE(pt.total_po_amount, 0) AS total_po_amount, COALESCE(it.total_invoice_amount, 0) AS total_invoice_amount, (COALESCE(it.total_invoice_amount, 0) - COALESCE(pt.total_po_amount, 0)) AS gap FROM site_vendor_assignments sva JOIN invoice_totals it ON sva.site = it.site LEFT JOIN po_totals pt ON sva.site = pt.site AND it.year = pt.year AND it.month = pt.month WHERE sva.is_primary = 'Y' AND EXISTS ( SELECT 1 FROM sales_orders so WHERE so.site = sva.site AND so.status = 'Open' ) AND (pt.year IS NOT NULL OR it.year IS NOT NULL);` — sil.quantity does not exist on table `sales_invoice_lines`
- Outcome after 2 correction attempt(s): STILL BROKEN — `WITH po_totals AS ( SELECT po.site, EXTRACT(YEAR FROM po.po_date) AS year, EXTRACT(MONTH FROM po.po_date) AS month, SUM(pol.qty_ordered * pol.unit_cost) AS total_po_amount FROM purchase_orders po JOIN purchase_order_lines pol ON po.po_id = pol.po_id GROUP BY po.site, year, month ), invoice_totals AS ( SELECT so.site, EXTRACT(YEAR FROM si.invoice_date) AS year, EXTRACT(MONTH FROM si.invoice_date) AS month, SUM(sil.unit_price * sil.quantity) AS total_invoice_amount FROM sales_invoices si JOIN sales_invoice_lines sil ON si.invoice_id = sil.invoice_id JOIN sales_orders so ON si.order_id = so.order_id WHERE so.status = 'Open' GROUP BY so.site, year, month ) SELECT sva.site, COALESCE(pt.total_po_amount, 0) AS total_po_amount, COALESCE(it.total_invoice_amount, 0) AS total_invoice_amount, (COALESCE(it.total_invoice_amount, 0) - COALESCE(pt.total_po_amount, 0)) AS gap FROM site_vendor_assignments sva JOIN invoice_totals it ON sva.site = it.site LEFT JOIN po_totals pt ON sva.site = pt.site AND it.year = pt.year AND it.month = pt.month WHERE sva.is_primary = 'Y' AND EXISTS ( SELECT 1 FROM sales_orders so WHERE so.site = sva.site AND so.status = 'Open' ) AND (pt.year IS NOT NULL OR it.year IS NOT NULL);`
- Suggested wiki fix: `entities/purchase_orders` was NOT among the pages used, so its real column list wasn't in context when the query was written — check whether pages linking to `purchase_orders` surface its key columns directly.

## [2026-08-03] purchase_orders — validation failed on first attempt

- Question: "Which sites have both a site_vendor_assignments primary vendor AND active sales_orders, and what's the gap between what they buy (PO $) and what they sell (invoice $) in the same month?"
- Wiki pages used: entities/site_vendor_assignments, entities/sales_orders, entities/purchase_orders, entities/sales_invoices, queries/unshipped-items-purchase-orders, relationships/overview, entities/sites, entities/source_vendor_pricing
- Rejected SQL: `WITH po_totals AS ( SELECT po.site, DATE_TRUNC('month', po.po_date) AS month, SUM(po.total_amount) AS total_po_amount FROM purchase_orders po GROUP BY po.site, month ), invoice_totals AS ( SELECT so.site, DATE_TRUNC('month', si.invoice_date) AS month, SUM(si.total_amount) AS total_invoice_amount FROM sales_invoices si JOIN sales_orders so ON si.order_id = so.order_id WHERE so.status = 'Open' GROUP BY so.site, month ) SELECT sva.site, COALESCE(pt.total_po_amount, 0) AS total_po_amount, COALESCE(it.total_invoice_amount, 0) AS total_invoice_amount, (COALESCE(it.total_invoice_amount, 0) - COALESCE(pt.total_po_amount, 0)) AS gap FROM site_vendor_assignments sva JOIN invoice_totals it ON sva.site = it.site LEFT JOIN po_totals pt ON sva.site = pt.site AND it.month = pt.month WHERE sva.is_primary = 'Y' AND EXISTS ( SELECT 1 FROM sales_orders so WHERE so.site = sva.site AND so.status = 'Open' );`
- Validation error: po.total_amount does not exist on table `purchase_orders`
- All rejected attempts:
  1. `WITH po_totals AS ( SELECT po.site, DATE_TRUNC('month', po.po_date) AS month, SUM(po.total_amount) AS total_po_amount FROM purchase_orders po GROUP BY po.site, month ), invoice_totals AS ( SELECT so.site, DATE_TRUNC('month', si.invoice_date) AS month, SUM(si.total_amount) AS total_invoice_amount FROM sales_invoices si JOIN sales_orders so ON si.order_id = so.order_id WHERE so.status = 'Open' GROUP BY so.site, month ) SELECT sva.site, COALESCE(pt.total_po_amount, 0) AS total_po_amount, COALESCE(it.total_invoice_amount, 0) AS total_invoice_amount, (COALESCE(it.total_invoice_amount, 0) - COALESCE(pt.total_po_amount, 0)) AS gap FROM site_vendor_assignments sva JOIN invoice_totals it ON sva.site = it.site LEFT JOIN po_totals pt ON sva.site = pt.site AND it.month = pt.month WHERE sva.is_primary = 'Y' AND EXISTS ( SELECT 1 FROM sales_orders so WHERE so.site = sva.site AND so.status = 'Open' );` — po.total_amount does not exist on table `purchase_orders`
- Outcome after 0 correction attempt(s): fixed — `WITH po_totals AS ( SELECT po.site, DATE_TRUNC('month', po.po_date) AS month, SUM(po.total_amount) AS total_po_amount FROM purchase_orders po GROUP BY po.site, month ), invoice_totals AS ( SELECT so.site, DATE_TRUNC('month', si.invoice_date) AS month, SUM(si.total_amount) AS total_invoice_amount FROM sales_invoices si JOIN sales_orders so ON si.order_id = so.order_id WHERE so.status = 'Open' GROUP BY so.site, month ) SELECT sva.site, COALESCE(pt.total_po_amount, 0) AS total_po_amount, COALESCE(it.total_invoice_amount, 0) AS total_invoice_amount, (COALESCE(it.total_invoice_amount, 0) - COALESCE(pt.total_po_amount, 0)) AS gap FROM site_vendor_assignments sva JOIN invoice_totals it ON sva.site = it.site LEFT JOIN po_totals pt ON sva.site = pt.site AND it.month = pt.month WHERE sva.is_primary = 'Y' AND EXISTS ( SELECT 1 FROM sales_orders so WHERE so.site = sva.site AND so.status = 'Open' );`
- Suggested wiki fix: `entities/purchase_orders` WAS in context but the model still invented a column/table — check whether its Column Reference Table, Common Query Examples, or Known Query Gotchas clearly distinguish similarly-named columns.
